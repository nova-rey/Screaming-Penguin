# Rootfs Directory

This directory is reserved for notes and helper files related to building and managing the Debian rootfs tarball used by Screaming Penguin.

The actual rootfs tarball used by the installer is expected to live on the USB's `/config` partition at:

- `/config/rootfs/debian-rootfs.tar.gz`
