Installer kernel detection now runs through `_determine_kernel_version`, which honors overrides, runtime-chroot module directories, and symlink names while emitting the chosen method/version/modules before staging.

- Added `SP_INSTALLER_RUNTIME_CHROOT_MODULES` plus the `_determine_kernel_version` helper to enforce the override → runtime-chroot → symlink priority (and hard errors on conflicts or missing runtime-chroot content) as shown around `tools/build_installer_initramfs.sh:9` and `:34`.
- The detection block now resolves the installer image, logs the method/version/module paths, and respects `SP_INSTALLER_KERNEL_DETECT_MODE` before waterboarding `build/installer-initramfs`, so tests can exercise only the detection logic (`tools/build_installer_initramfs.sh:90`, `:112`, `:118`).
- Module staging continues to copy `MODULES_SRC` → `MODULES_DST` but now logs both paths right before the copy and still fails fast if the configured directory is absent (`tools/build_installer_initramfs.sh:255`).
- Tests now drive detection-only runs via the new helper and cover override (`tests/installer/test_kernel_module_identity.py:31`), runtime-chroot selection (`:55`), conflicting versions (`:80`), and the fallback failure path (`:100`), all wired through the shared `_run_detection` helper (`:17`) that sets `SP_INSTALLER_KERNEL_DETECT_MODE`.

- Testing: `shellcheck tools/build_installer_initramfs.sh`; `python -m compileall -q .` (fails: `python` binary missing, so reran as `python3 -m compileall -q .`).

Next step: run `python3 -m pytest tests/installer/test_kernel_module_identity.py` to exercise the new detection tests end-to-end.