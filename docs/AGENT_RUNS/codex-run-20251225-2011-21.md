- SCSI rescan now emits warning-only logs rather than failing when `/sys/class/scsi_host/…/scan` is read-only, thanks to the new `sp_log_warn` helper and optional `SP_PREPEND_PATH` hook in the entrypoints (`installer/init/init.sh:53`, `installer/init/init.sh:574` plus the identical mirror in `build/installer-initramfs/init`), keeping the build and runtime init scripts aligned while still allowing tests to prepend fake binaries.
- The storage bootstrap helper now skips unwritable scan paths, logs distinct `scan-not-writable`/`scan-write-failed` warnings, and continues without triggering rescue mode (`installer/runtime/lib/storage_bootstrap.sh:92` and the initramfs copy), ensuring CI keeps running even when sysfs is unwritable.
- `tests/installer/test_init_write_gate.py` now creates a module-root symlink for the current kernel (so the bootstrap check passes) and injects a `mount` stub that succeeds for `-t vfat` while respecting the new `SP_PREPEND_PATH` hook (`tests/installer/test_init_write_gate.py:28`), letting the VFAT probe complete without CAP_SYS_ADMIN.
- Tests: `shellcheck -x installer/init/init.sh build/installer-initramfs/init`, `pytest -q tests/installer/test_init_write_gate.py::test_init_allows_true_write_gate`, `make ci-smoke`.
- Notes: Running the smoke suite rewrote tracked artifacts (`dist/initrd-installer.img`) and populated `__pycache__` files; please reset those entries if you don’t want them in the diff.

Next steps:
1. Clean or keep the generated `dist`/`__pycache__` files as needed before committing so they don’t leak into the change set.
2. If you need to validate this on a different CI kernel, rerun `make ci-smoke` there now that VFAT/mount and SCSI warnings are covered.