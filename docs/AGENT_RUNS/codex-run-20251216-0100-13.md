CI smoke now installs `python3` (via apt) and `pyfatfs` alongside the other tooling before running `make ci-smoke`, so the installer bootability tests have their required dependency (`.github/workflows/ci.yml:37`, `.github/workflows/ci.yml:39`).  
Added a quick `pyfatfs` import check right before the pytest run to make missing-dependency failures obvious (`tools/ci_smoke.sh:20`).  
Tests: `shellcheck tools/ci_smoke.sh` (pass)

Next steps:  
1. Run `make ci-smoke` to ensure the full smoke suite passes with the new dependency.