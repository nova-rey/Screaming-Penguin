- Added scaffolding docs and metadata that describe the RAM-resident Ouroboros installer goals, safety constraints, and structure so future contributors immediately understand the intent of this branch (`README.md:1`, `docs/ouroboros-overview.md:1`).
- Captured stubbed detection, sanity, and re-image orchestration scripts that document the expected flow while keeping all destructive commands commented and gated, ready for later enablement (`scripts/detect_boot_device.sh:1`, `scripts/sanity_checks.sh:1`, `scripts/reimage_usb_from_ram.sh:1`).
- Provided the minimal initramfs `init` and bash-based ISO-builder scaffold to show how the RAM boot process and ISO build pipeline should invoke these scripts without yet producing artifacts (`initramfs_root/init:1`, `tools/make_ouroboros_iso.sh:1`).
- Created the required directories plus `.gitkeep` placeholders so the build/dist trees persist in version control even while empty (`build/.gitkeep:1`, `dist/.gitkeep:1`).

No automated tests were run (scaffolding only). Next steps you might take: 1) Implement the actual initramfs build flow and confirm the scripts execute in a controlled RAM environment. 2) Wire the ISO-builder stub into your standard build tools (kernel/initramfs copying + xorriso). 3) Later, once detection/confirmation are validated, uncomment the destructive commands under strict safeguards.