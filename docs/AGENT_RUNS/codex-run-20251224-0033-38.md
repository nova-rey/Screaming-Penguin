Ensured the installer now verifies `/dev/disk/by-label` before discovery, logs the new statuses and diagnostics, and covers the behavior with targeted tests.
- `installer/init/init.sh` now checks the namespace after `sp_populate_disk_by_label`, retries population once, prints `[SP-INSTALLER] by-label ready` when entries appear, and emits `[SP-INSTALLER][WARN] by-label empty after population attempt` if it still stays empty.
- `installer/runtime/lib/config_discovery.sh` fails fast if the by-label directory is missing/empty, logs `[SP-INSTALLER][FATAL] by-label namespace empty after population`, and immediately enters rescue using the `by-label-empty` reason before any label-based discovery.
- `installer/runtime/lib/rescue_mode.sh` now dumps `ls -l /dev/disk/by-label`, `blkid`, and `/proc/partitions` whenever rescue is triggered for that by-label failure, keeping the existing diagnostics chain afterward.
- Added `tests/installer/test_config_discovery_busybox.py` coverage to prove discovery succeeds when labels are present and that rescue fires only when the namespace stays empty.

Tests:
- `pytest tests/installer/test_initramfs_by_label_population.py`
- `shellcheck -x installer/init/init.sh installer/runtime/lib/config_discovery.sh`
- `shellcheck -x installer/runtime/lib/rescue_mode.sh`

Next steps:
1. Run `pytest tests/installer/test_config_discovery_busybox.py` to validate the new guard and rescue paths.