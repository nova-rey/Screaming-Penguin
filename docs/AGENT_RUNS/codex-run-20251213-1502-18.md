- Strengthened config discovery by pushing the logic into the new `installer/runtime/lib/config_discovery.sh:1` helper, which now prefers `/dev/disk/by-label/SP_CONFIG`, scans every VFAT partition listed by `blkid`, tracks every candidate attempt, retries with `udevadm settle`, logs `lsblk -f`/`blkid` output plus the candidate summary before calling `sp_enter_rescue_mode`, and keeps `/config/installer-config.yml` read-only mounted so PID 1 can drop into an interactive shell instead of exiting (`installer/runtime/lib/config_discovery.sh:232`, `:266`, `:276`, `:62`). `installer/init/init.sh:35` now sources that helper and `main()` (lines `685-705`) unconditionally enters the graceful rescue mode when `/config` is still missing, ensuring the write gate stays blocked instead of panicking.
- Added deterministic tests and supporting mocks: `tests/installer/test_config_discovery.py:34`, `:72`, `:105` now assert label preference, VFAT fallback, and rescue-path behavior while using stubs under `tests/installer/bin/mount:1` (and siblings for `blkid`, `lsblk`, `udevadm`, `umount`) so no real devices are required, and `tests/installer/test_installer_media_bootability.py:260` now stages/disturbs the placeholder kernel/initrd before rebuilding the ISO so that the iso-initrd identity assertion always runs on the generated artifact.
- Tests: `pytest -q`, `ruff check .`, `black --check installer tools tests`, `shellcheck -x tools/*.sh installer/init/init.sh installer/runtime/lib/*.sh` (all clean).
- Next: consider exercising the updated discovery/rescue path on physical hardware to confirm it still mounts the correct partition and leaves a usable shell when the config is absent.