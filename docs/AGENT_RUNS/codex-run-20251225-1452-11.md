Config discovery now trusts `blkid -o export` for label resolution, logs a warning whenever `/dev/disk/by-label` is missing/empty, and only enters rescue when `blkid` itself is unusable or no matching label exists.

- `installer/runtime/lib/config_discovery.sh:147` introduces `sp_build_label_map`/`sp_lookup_label_device`, builds a LABEL→DEVNAME map, and keeps `SP_CONFIG_LABEL_PROBE_CANDIDATES` in sync without iterating `/proc/partitions`. `installer/runtime/lib/config_discovery.sh:394` reroutes `sp_try_label_candidate` so by-label symlinks remain a fast path but everything falls back to the new map (with `[SP-INSTALLER][WARN] by-label directory unavailable; falling back to blkid`) and only hard-fails when `blkid` is missing or the requested label isn’t in the map.

- `tests/installer/test_config_discovery_busybox.py:372`/`:396` replace the old fail-on-empty-by-label expectation with two new scenarios that prove discovery still succeeds when the namespace is missing or empty (and emits the new warning) while continuing to rely on the stubbed `blkid` output.

- `installer/init/init.sh:30`/`:1080` now track `SP_BOOTSTRAP_COMPLETE`, re-run `sp_bootstrap` if necessary before discovery, and export that state so `sp_run_installer` only touches config once device bootstrap (including `mdev`) has finished; the same updates were mirrored into `build/installer-initramfs/runtime/lib/config_discovery.sh:147` and `build/installer-initramfs/init:38`/`:1080` so the initramfs payload stays in sync.

Testing:
- `shellcheck -x installer/init/init.sh installer/runtime/lib/config_discovery.sh` (pass)
- `pytest -q tests/installer/test_config_discovery_busybox.py` (pass, 17 tests)

Next steps:
1. Optionally run `pytest tests/installer/test_bootstrap_dev_nodes.py::test_bootstrap_runs_mdev_before_config_discovery` to reassure the bootstrap ordering test still behaves as expected.
2. If you’re producing new installer artifacts, rebuild the initramfs so the updated helper/guard land inside the shipped image.