Added a USB-only policy layer so the initramfs aborts whenever a non-USB block device leaks through and every boot target is validated via sysfs before destructive logic runs.

- Implemented `ouroboros/scripts/assert_usb_only_environment.sh:4` to walk `/sys/class/block`, skip `virtual/` devices, identify USB ancestry via `ID_BUS`/`/usbX/`, and exit with descriptive errors if any non-USB nodes or symlinks are exposed; the same helper now supports `--check-device` so downstream scripts can gate each candidate before touching it.
- Updated `ouroboros/scripts/reimage_usb_from_ram.sh:8`/`21` to call the assertion before detecting the boot device and again to make sure the found device is USB-backed, so the reimage flow never advances once the policy is violated.
- Ensured `ouroboros/initramfs_root/init:12` invokes the USB assertion before running the reimage script, preventing Phase 3 from ever seeing a forbidden `/dev` snapshot.
- Captured the policy intent in `ouroboros/policy/usb-only.rules:1` (udev/mdev hint stanza) and documented the analysis + Block C verification status in `docs/analysis/p2-c1-analysis.md:38`.
  
Tests/verification:  
- `bash tools/make_ouroboros_iso.sh` (the builder is currently a scaffold that stops before producing `sp-ouroboros-installer.iso`, so no ISO exists to boot and the requested QEMU runs cannot yet be executed).  

Next steps for the user:  
1. Replace the stub ISO builder with a real workflow that stages the initramfs + kernel and emits a bootable `sp-ouroboros-installer.iso`.  
2. Once the ISO exists, boot it under QEMU with both internal and USB-backed block devices to confirm the USB-only policy and abort behavior.  
3. Hook the policy hints in `ouroboros/policy/usb-only.rules` up to the real device manager (udev/mdev) in the initramfs image when that infrastructure is ready.