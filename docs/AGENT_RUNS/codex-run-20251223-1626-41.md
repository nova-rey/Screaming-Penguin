**By-label population**
- Added `sp_populate_disk_by_label` and call it immediately before discovery so `blkid -o export` drives `/dev/disk/by-label` symlinks before config lookups; see `installer/init/init.sh:490` and the invocation at `installer/init/init.sh:1060`.
- Warn when the by-label namespace is still absent at discovery start so logs point straight to the population gap (`installer/runtime/lib/config_discovery.sh:655`).
- Tested the built initramfs for the helper, `blkid` usage, and the directory creation logic (`tests/installer/test_initramfs_by_label_population.py:1`).
- Summarized the scenario, root cause, and verification steps in a new analysis doc (`docs/analysis/mp-fix-by-label-population.md:1`).

Tests: not run (not requested).

Next steps:
1. Run `pytest tests/installer/test_installer_initramfs_by_label_population.py tests/installer/test_installer_initrd.py` to confirm the initrd layout and new helper are present.