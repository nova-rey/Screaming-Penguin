- Bound the smoke pytest run to a configurable timeout and to a fast-lane test selection (with `CI_SMOKE_FULL_INSTALLER=1` optionally keeping the full installer run), so `tools/ci_smoke.sh:22-39` now sets defaults, adjusts `PYTEST_ARGS`, and invokes `timeout … pytest` around the installer targets.
- Added `CI_SMOKE_PYTEST_TIMEOUT_SECONDS` and `CI_SMOKE_FULL_INSTALLER` defaults in `Makefile:3-48` so invoking `make ci-smoke` keeps the new knobs available even without explicit env overrides.
- Propagated the same defaults into the PR CI job (`.github/workflows/ci.yml:17-46`) so the GitHub runner also uses the bounded fast lane by default.

Testing: Not run (not requested).

Next steps for verification:
1. Run `make ci-smoke` locally once to ensure the fast-lane + timeout workflow passes.