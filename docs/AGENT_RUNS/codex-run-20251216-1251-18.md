CI smoke now installs the needed Python tooling, runs verbose pytest progress (with a collect-only gate), and documents the bounded behavior so the last-emitted test name is always visible before the outer timeout.

- **CI tooling** `.github/workflows/ci.yml:17` now exports `PYTHONUNBUFFERED=1` alongside `CI_SMOKE_PYTEST_TIMEOUT_SECONDS`, and the tooling step installs `pytest`, `pytest-timeout`, and `pyfatfs` by pip so the smoke lane has everything it needs without touching ISOs (`.github/workflows/ci.yml:40`).
- **Tools script** `tools/ci_smoke.sh:4` exports `PYTHONUNBUFFERED=1`, the pytest invocation now uses chatty args plus optional `--timeout=60 --timeout-method=thread` when `pytest-timeout` exists (`tools/ci_smoke.sh:26`–`tools/ci_smoke.sh:51`), and a collection-only pass precedes the bounded run so any import/dependency issues fail fast (`tools/ci_smoke.sh:53`–`tools/ci_smoke.sh:60`).
- **Docs & logs** `docs/CI.md:12` now explains the new unbuffered/collect-only/timeout behavior and points out that the last pytest progress line is the one to inspect, while `docs/analysis/sp-mp-ci-trace-hang-analysis.md:1` captures the deeper reasoning, test classification, and historical note about why pytest is chatty.
- **Verification** Recorded the runs in `docs/analysis/sp-mp-ci-trace-hang-verification.md:3`; `shellcheck tools/*.sh` and `python3 -m compileall -q .` passed, and `CI_SMOKE_PYTEST_TIMEOUT_SECONDS=60 bash tools/ci_smoke.sh` exercised the new progress logging but aborted when `tests/installer/test_bootstrap_dev_nodes.py::test_bootstrap_runs_mdev_before_config_discovery` failed because `/dev/console` and matching kernel modules are inaccessible in this sandbox (`docs/analysis/sp-mp-ci-trace-hang-verification.md:8`–`docs/analysis/sp-mp-ci-trace-hang-verification.md:21`).
- **Next steps** Re-run `tools/ci_smoke.sh` in an environment that can reach `/dev/console` and the matching `/lib/modules` (or skip that acceptance-y test) so the bounded suite is exercised end to end, and keep an eye on the verification doc’s note that `pytest-timeout` isn’t installed locally but will be provided by the workflow (`docs/analysis/sp-mp-ci-trace-hang-verification.md:23`).