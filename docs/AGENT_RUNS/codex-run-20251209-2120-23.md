Image tooling now keeps its temporary workspaces separate from the shared `build/runtime` tree and the installer media tests assert the runtime artifacts survive, while the ISO builder can fall back to dist-provided kernels/initrds.

- **Image builder cleanup:** `tools/make_installer_img.sh` now runs under `bash -euo pipefail`, defines dedicated `SP_IMG_BUILD_DIR`/`SP_IMG_MNT_DIR`, and only removes those paths so `build/runtime` stays intact while mounting the FAT32 ESP (`tools/make_installer_img.sh:5`, `tools/make_installer_img.sh:11`, `tools/make_installer_img.sh:152`).
- **ISO resilience:** `tools/make_installer_iso.sh` selects `SP_BOOT_KERNEL`/`SP_BOOT_INITRD` from either `build/runtime` or falling back to `dist/` before copying them into the ISO tree and the dist artifacts (`tools/make_installer_iso.sh:29`, `tools/make_installer_iso.sh:211`).
- **Installer tests:** the FAT32 ESP smoke test now asserts `build/runtime/vmlinuz` still exists after the `.img` run and a new `test_make_installer_iso_falls_back_to_dist_artifacts` ensures `make_installer_iso.sh` succeeds with only dist kernels/initrds while skipping when `xorriso`/`grub-mkstandalone` are unavailable (`tests/installer/test_installer_media_bootability.py:72`, `tests/installer/test_installer_media_bootability.py:96`, `tests/installer/test_installer_media_bootability.py:106`).
- **Makefile:** the `img` target now invokes `bash tools/make_installer_img.sh`, matching the script’s shell requirement and avoiding fallback to `/bin/sh` (`Makefile:9`).
- **Verification:** `pytest -q`, `ruff check .`, `black --check installer tools tests`, and `shellcheck -x tools/*.sh installer/init/init.sh installer/runtime/lib/*.sh` all pass after these updates.