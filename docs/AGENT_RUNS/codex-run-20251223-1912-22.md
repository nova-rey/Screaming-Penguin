**Changes**
- `installer/init/init.sh:490-497` now has `sp_populate_disk_by_label()` simply create `/dev/disk/by-label`, invoke `blkid -o export` when available, and log `[SP-INSTALLER] by-label populated` once population succeeds so the helper only ensures the namespace exists and is primed for label-based discovery just before that logic runs.
- `installer/runtime/lib/config_discovery.sh:655-662` now checks `/dev/disk/by-label` at discovery start and, if the directory exists but has no entries, emits `[SP-INSTALLER][WARN] by-label namespace empty after population` (while still warning when the directory is missing) so failures in the helper are surfaced without changing discovery flow.

**Verification**
- `shellcheck installer/init/init.sh installer/runtime/lib/config_discovery.sh` (this run still reports the pre-existing SC1091/SC2086/SC2034 items around sourced libs and the unquoted `set -- $line`; no new warnings were introduced by these edits).

Next: test the installer on real hardware to confirm the new by-label log appears and that discovery proceeds without hitting rescue mode.