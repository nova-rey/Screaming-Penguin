- **Console/serial resilience:** new helpers near `installer/init/init.sh:6` compute `SP_OUT_DEVICE`, gate `/dev/ttyS0` writes, and drive the init marker through the best-available log path; `sp_idle_shell` now skips the hardened `/dev/console` redirect when it isn’t writable so the bootstrap never exits just because of permission errors.  
- **Rescue-mode guardrail:** `installer/runtime/lib/rescue_mode.sh:7` now exposes `sp_is_ci`, a `sp_rescue_console_fd_setup` that falls back to stderr, and a CI-only early exit so rescue mode doesn’t loop/spam when `/dev/console` can’t be opened.  
- **Analysis note:** captured the CI hang root cause and the hardened call sites in `docs/analysis/mp-console-perms-hang.md:1`.  
- **Tests:** `bash tools/ci_smoke.sh` (fails because the installer aborts early with `[SP-INSTALLER] reason=modules-missing`/`kernel/modules mismatch`—the container lacks `/lib/modules/6.8.12-13-pve`), `pytest -q tests/installer/test_bootstrap_dev_nodes.py -k mdev -vv` (same fatal kernel-module mismatch).  

Next steps:  
1. Re-run the smoke suite and targeted pytest once a matching `/lib/modules/$(uname -r)` tree is available so the kernel/modules validation can succeed and we can confirm the console fixes on a real bootstrap.