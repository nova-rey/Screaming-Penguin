- Tightened the initrd label test so it imports `re` and asserts on `("?$blkid_bin"?|blkid)\s+-o\s+export\b`, letting the check pass whether `blkid` is invoked directly or via a variable (`tests/installer/test_initramfs_by_label_population.py:26`).
- Each dynamic include in `installer/init/init.sh` now carries `# shellcheck disable=SC1091` plus the appropriate `# shellcheck source=…` metadata so lint knows where the helper scripts live (`installer/init/init.sh:93`).
- `installer/runtime/lib/config_discovery.sh` now annotates its helper sources (`installer/runtime/lib/config_discovery.sh:17`), exports `SP_CONFIG_MOUNT_TRIED_VFAT` for other scripts (`installer/runtime/lib/config_discovery.sh:57`), and parses `/proc/partitions` lines via a safe `read` block instead of `set --` to satisfy SC2086/SC2034 without behavioral change (`installer/runtime/lib/config_discovery.sh:216`).

- **Tests**
- `pytest -q tests/installer/test_initramfs_by_label_population.py::test_installer_initrd_populates_disk_by_label tests/installer/test_installer_initrd.py`
- `shellcheck installer/init/init.sh installer/runtime/lib/config_discovery.sh tools/build_installer_initramfs.sh`