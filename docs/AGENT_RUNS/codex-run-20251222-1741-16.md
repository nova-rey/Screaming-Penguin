Added `_depmod_version_is_numeric` to treat kernel versions that start with a digit as depmod candidates, and only run `depmod -b …` when that helper succeeds; any override that fails the gate now skips depmod with a warning instead of aborting the build (`tools/build_installer_initramfs.sh:36` and `tools/build_installer_initramfs.sh:320`).

Documented the observed failure and decision to gate depmod in `docs/analysis/mp-fix-depmod-invalid-kernel-version.md:1` so the rationale is preserved alongside the change.

Tests:
- `pytest -q tests/installer/test_installer_initrd.py::test_installer_initrd_contains_runtime_payload` (pass)
- `pytest -q tests/installer/test_initramfs_runtime_lib_path.py::test_local_runtime_lib_has_priority` (pass)

Next step: run a real-kernel installer initrd build or broader installer CI when a valid kernel version is available to exercise the numeric depmod path in production.