Rootfs deployment now lives in a dedicated runtime helper: `installer/runtime/lib/rootfs_deploy.sh#L1-L494` reads `installer.rootfs.*`, mounts the new root partition (with `SP_ROOTFS_TARGET_DIR_OVERRIDE` for tests), extracts `/config/os/rootfs.tar.gz`, bind-mounts the virtual filesystems, and conditionally runs hostname/timezone/locale/user/SSH configuration (honoring `SP_SKIP_ROOTFS_DEPLOY`, `SP_SKIP_CHROOT_CONFIG`, and `SP_DEBUG_ROOTFS`) before unmounting everything and closing the `[SP-INSTALLER] rootfs` span. `installer/init/init.sh#L742-L775` sources the helper and invokes `sp_rootfs_deploy_and_configure` right after a successful disk execution so Phase 11 hooks immediately after Phase 10 whenever `SP_MODE=INSTALL`.

Configs and docs now describe the new block: `config/installer-config.example.yml#L1-L47` showcases `installer.rootfs` defaults, `docs/CONFIG_SCHEMA.md#L11-L115` explains the schema plus the new Phase 11 section and CI toggles, `installer/runtime/lib/config_validation.sh#L19-L45` reads the same keys (with fallbacks to the old sections), and the contract/architecture/roadmap materials (`docs/installer_contract.md#L10-L16`, `docs/architecture.md#L26-L36`, `docs/DEV_ROADMAP.md#L550-L560`, and the new `docs/Phase11_Roadmap.md#L1-L15`) now document the stage, `/config/os/rootfs.tar.gz`, and the skip/debug knobs. I also captured the reasoning in `docs/analysis/p11-rootfs-deploy-analysis.md#L1-L55`.

Tests now drive the helper in a non-destructive way: `tests/installer/test_rootfs_deploy.py#L1-L161` builds a tiny tarball, toggles the skip flags, and checks that extraction plus hostname/timezone/locale files are written into a temp tree without touching real devices. This adds coverage for the new environment overrides mentioned in the analysis note.

Tests:  
- `PATH=/tmp:$PATH pytest -q` (local `/tmp/yq` binary is in PATH so the helper can parse YAML).  
- `PATH=/tmp:$PATH ruff check .`  
- `PATH=/tmp:$PATH black --check installer tests`  
- `PATH=/tmp:$PATH shellcheck -x installer/init/init.sh installer/runtime/lib/*.sh`

Next steps: keep wiring Phase 11 into the broader installer flow (grub, logs) and align the live initramfs image builds with this stage now that the schema and docs are settled.