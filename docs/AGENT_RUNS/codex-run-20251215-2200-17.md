- **CI Smoke** – `.github/workflows/ci.yml:1` now hosts the short-lived PR/push job that installs shellcheck/pytooling, then runs `make ci-smoke` (which was added to `Makefile:6` and wraps `tools/ci_smoke.sh:1`). That helper prints stage markers, runs `shellcheck tools/*.sh`, `python3 -m compileall -q .`, `pytest tests/installer`, and optional `ruff`/`black` checks when configs are present, keeping the PR lane focused on fast validation.
- **Bounded ISO Build** – `ci-iso-build` ( `.github/workflows/ci-iso-build.yml:1`) is now a manual/nightly workflow that installs the heavy toolchain, writes heartbeat-aware logs via `tools/ci_heartbeat.sh:1`, and executes the three-stage sequence wrapped by `tools/ci_iso.sh:1`. The ISO step sets `SP_SKIP_INSTALLER_INITRAMFS_BUILD=1` so `tools/make_installer_iso.sh:28` does not rebuild the initramfs, and every stage appends to `ci-debug-summary.txt` before artifact upload, giving visibility before the 150‑minute timeout hits.
- **Docs & Analysis** – Added `docs/CI.md:1` to explain the smoke vs. full workflows and how to run them locally (`make ci-smoke`/`make ci-iso`), and captured the reasoning and hang-point triage in `docs/analysis/sp-mp-ci-dejankify-analysis.md:1` so future agents understand the de-jankification requirements.

- Tests: `shellcheck -x tools/ci_heartbeat.sh tools/ci_smoke.sh tools/ci_iso.sh tools/make_installer_iso.sh` (pass); `python3 -m compileall -q .` (pass); `pytest tests/installer` (fails because `installer/init/init.sh` cannot write `/dev/console` and reports a kernel/modules mismatch in this sandboxed runner, so the suite aborts before completion).

Next steps:
1. Re-run `pytest tests/installer` in an environment that provides `/dev/console` and matching `/lib/modules` (or gate the failing test) so the smoke suite can finish cleanly.
2. Trigger the manual “CI ISO Build” workflow (or wait for the scheduled run) to verify the heartbeat/timeouts, artifacts, and `ci-debug-summary.txt` behave as expected before relying on the nightly cadence.