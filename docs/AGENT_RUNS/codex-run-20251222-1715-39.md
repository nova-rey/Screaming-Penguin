- Wrapped the `tools/build_installer_initramfs.sh` invocation inside `tests/installer/test_installer_initrd.py` with a `try/except` so a failing build now raises `pytest.fail` that reports captured stdout/stderr for easier CI diagnosis (`tests/installer/test_installer_initrd.py:49`).
- **Tests:** Not run (not requested).

**Next Steps**
1. Run `pytest -q tests/installer/test_installer_initrd.py::test_installer_initrd_contains_runtime_payload` to confirm the initramfs build still succeeds and to see the richer failure output if it doesn’t.