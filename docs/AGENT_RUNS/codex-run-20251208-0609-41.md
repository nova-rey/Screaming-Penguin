Introduced a non-destructive GPT planner that reads config/yaml, selects the target disk, and emits a deterministic EFI+root JSON plan without touching any devices yet.

- Added `installer/runtime/lib/disk_layout.sh:38–199` (Python-backed YAML reader, disk selection, sizing helpers, and `sp_plan_gpt_layout`/`sp_print_layout_plan`) so the planner knows how to build EFI/root entries from `installer.disk_layout` tuning knobs before any writes happen.
- `installer/init/init.sh:31–38` now sources that planner, and `installer/init/init.sh:701–706` short-circuits into a `[SP-INSTALLER] disk-layout plan START/...END` section (both console + serial) whenever `SP_DEBUG_DISK_LAYOUT=1`, ensuring the plan surface is visible while the write gate stays on.
- Config/docs now describe the new bits: `config/installer-config.example.yml:8` seeds `installer.disk_layout`, `docs/CONFIG_SCHEMA.md:8` explains the keys and JSON plan, `docs/installer_contract.md:10`/`docs/architecture.md:15` mention the planning stage and tracers, Phase 9 is recorded in `docs/Phase9_Roadmap.md:1` and `docs/DEV_ROADMAP.md:526`, and the repo survey/plan lives at `docs/analysis/p9-c1-disk-layout-analysis.md:1`.
- Added `tests/installer/test_disk_layout_planner.py:1` to exercise the planner’s happy path, config overrides, and failure cases by invoking `sp_print_layout_plan` in a fake runtime.

Testing:
- `pytest -q`
- `ruff check .`
- `black --check installer tests`

Next steps:
1. Wire this plan into the execute phase so GPT creation and mkfs commands apply the JSON plan once the write gate is intentionally opened.