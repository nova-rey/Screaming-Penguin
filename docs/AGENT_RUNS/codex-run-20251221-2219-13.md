- Tightened the label-probing helpers so `sp_has_blkid` now exercises the discovered `blkid` binary (runs `--version`) and `sp_try_label_candidate` captures the `sp_find_partition_by_fs_label` failure code before emitting fatal markers, ensuring missing-`blkid` situations log `missing-blkid-for-label-probe` even when no partition entries exist; the initramfs copy is now identical to the runtime helper (`installer/runtime/lib/config_discovery.sh:94-424`, `build/installer-initramfs/runtime/lib/config_discovery.sh:94-424`).
- Removed the temporary PATH debug print from the BusyBox test helper so `_run_command` stays quiet while still prepending the stub bin directory (`tests/installer/test_config_discovery_busybox.py:14-32`).
- Tests: `pytest -q tests/installer/test_config_discovery_busybox.py`.