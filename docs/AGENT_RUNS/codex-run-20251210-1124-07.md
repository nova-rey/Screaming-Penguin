Swapped the installer image layout so `/config` lives on partition 1 and the ESP moves to partition 2, then locked that ordering into the test harness and documentation.

- **Image builder** now creates the CONFIG FAT32 volume first, then the ESP, formats and labels them accordingly, and mounts the ESP (not p1) when populating the boot tree (`tools/make_installer_img.sh:71`‑`138`).
- **Tests** share a helper to stage the stub artifacts, invoke the builder under `sudo`, and inspect partition metadata via `sgdisk`, then open both the ESP and CONFIG slices through `PyFatFS` to assert FAT32 plus label/type expectations (`tests/installer/test_installer_media_bootability.py:41`‑`180`).
- **Docs** describe the config-first layout and its rationale across the usage guides and design notes (`docs/INSTALLER_USAGE.md:11`, `docs/CI_OVERVIEW.md:44`, `docs/RUNTIME_BOOT_DESIGN.md:22`, `docs/DEV_ROADMAP.md:123`, `docs/CONFIG_SCHEMA.md:104`, `docs/architecture.md:36`, `docs/V1_CHECKLIST.md:12`, `docs/GETTING_STARTED.md:80`).

- **Testing**: Not run (requires `sudo`/loop-device support). Please run the requested suite (`pytest -q`, `ruff check .`, `black --check installer tools tests`, `shellcheck -x tools/*.sh installer/init/init.sh installer/runtime/lib/*.sh`) plus `make img` once the environment can satisfy those requirements.