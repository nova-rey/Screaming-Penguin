Directory-backed partition candidates now short-circuit discovery by recognizing any directory that already contains `installer-config.yml`, setting `SP_CONFIG_PATH`/`CONFIG_MOUNT`, logging `phase=partition result=use-directory`, and returning success before mount attempts.

- `installer/runtime/lib/config_discovery.sh:359-376` detects directory candidates with `installer-config.yml`, exports the expected config path and mount-point variables, logs the new marker, and exits successfully so rescue mode is never triggered for these cases.
- Tests not run (not requested).

Next steps: 1. Run `make ci-smoke` (or at least `tests/installer/test_bootstrap_dev_nodes.py::test_bootstrap_runs_mdev_before_config_discovery`) to confirm the new flow stays green.