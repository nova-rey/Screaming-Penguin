- Added configurable console/serial wiring at `installer/init/init.sh:6`, introduced the `sp_enforce_write_gate` helper and `[SP-INSTALLER] write-gate OK/BLOCKED` markers at `installer/init/init.sh:128`, and now invoke the gate inside `main` (`installer/init/init.sh:634`) so missing/false `installer.write_gate` values abort before any disk logic runs while test-mode hooks (`SP_SKIP_CONFIG_DISCOVERY`, `SP_EXIT_AFTER_INIT`) keep the script cooperative during validation.
- Documented and enforced the new field by seeding `config/installer-config.example.yml:1`, calling out `installer.write_gate` inside `docs/CONFIG_SCHEMA.md:66`, and teaching `installer/runtime/lib/config_validation.sh:25` to parse/normalize it and reject configs where it isn’t `true`.
- Built a Python enforcement helper (`installer/python/write_gate.py:1`) and pytest coverage (`tests/installer/test_write_gate.py:1`, `tests/installer/test_init_write_gate.py:1`) with `tests/conftest.py:1` wiring the repo package path so both the helper and the init script are exercised under missing/false/true scenarios.
- Captured the Block A research in `docs/analysis/p8-c1-write-gate-analysis.md:1` and described the write-gate guarantee in `docs/installer_contract.md:5`, `docs/architecture.md:5`, and `docs/Phase8_Roadmap.md:1` so every stakeholder knows no disk writes may run unless the gate is true.

Tests:
- `pytest -q`
- `ruff check .`
- `black --check installer tests`

Next steps: when the installer eventually enables real disk writes, revalidate `installer.write_gate` right before the execute phase so the same gate still blocks unwanted wipes.