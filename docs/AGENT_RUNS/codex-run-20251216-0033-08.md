- Added `_cleanup` to `tools/ci_heartbeat.sh` so the heartbeat/command PIDs are killed inside a clearly callable function with inline `SC2317` disables, keeping the same semantics while satisfying ShellCheck (tools/ci_heartbeat.sh:40). Registered that handler with `trap _cleanup EXIT INT TERM` so the cleanup path is obviously reachable (tools/ci_heartbeat.sh:52).

- Tests: `shellcheck tools/ci_heartbeat.sh`; `shellcheck tools/*.sh` (both pass); `make ci-smoke` / `bash tools/ci_smoke.sh` exit 124 because `pytest tests/installer/test_bootstrap_dev_nodes.py` fails with `/dev/console: Permission denied` and the existing kernel/modules mismatch from `installer/init/init.sh` (reported at tests/installer/test_bootstrap_dev_nodes.py:94).

Next steps:
1. Investigate or adjust the installer bootstrap test environment so it can access `/dev/console` or skip that path in this CI context, resolving the current pytest failure.
2. Re-run `bash tools/ci_smoke.sh` (or `make ci-smoke`) once the installer test blocker is fixed to confirm the full smoke suite succeeds.