- `tools/make_installer_iso.sh:15` now defines the `SP_ISO_*` boot paths (and the runtime kernel selection stays in place), and the block around `tools/make_installer_iso.sh:40` invokes `tools/build_installer_initramfs.sh` before copying the resulting `dist/initrd-installer.img` plus the chosen kernel into those paths so the ISO’s GRUB stage always sees the busybox-backed initramfs it installs.
- `ci/qemu_smoke_ci.sh:48` now extracts `/boot/initrd-installer.img` into a `mktemp` workspace, uses `zcat | cpio -t -H newc` to capture the listing, logs the first 20 entries, and enforces the presence of `init` and `bin/busybox` before proceeding, with a trap to clean the temporary directory.
- `tests/installer/test_installer_iso_initramfs.py:1` adds a pytest that skips when xorriso/cpio/zcat or the ISO are missing, extracts the installer initramfs via xorriso, lists it via `zcat | cpio -t -H newc`, and asserts both `init` and `bin/busybox` are present so we can catch regressions without hitting QEMU.
- Tests: not run (not requested).

Next steps:
1. `pytest -q`
2. `ruff check .`
3. `black --check installer tools tests`
4. `shellcheck -x tools/*.sh installer/init/init.sh installer/runtime/lib/*.sh`