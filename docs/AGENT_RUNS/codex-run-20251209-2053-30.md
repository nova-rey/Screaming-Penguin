Shared runtime artifacts now build before both `.img` and `.iso`, and the supporting documentation records the new dependency story.

- `Makefile:3-29` now stitches `img`/`iso` onto a stamped `$(RUNTIME_DIR)/.installer-runtime-built` recipe that runs `tools/build_runtime.sh` + `tools/build_installer_initramfs.sh`, copies the kernel/initrd into `dist/`/`build/`, and exposes an `installer-runtime` helper so the runtime is only rebuilt when necessary.
- `docs/RUNTIME_BOOT_DESIGN.md:19-31` explains that `.img` now reuses the shared runtime kernel/initrd, and it spells out how `make runtime`, `make img`, `make iso`, and `make dist-release` coordinate around that step.
- `docs/analysis/p13-runtime-deps-analysis.md:1-20` captures the previous graph, the CI failure reason, the new installer-runtime responsibilities, and how `img`/`iso`/`dist-release` now link into a single runtime build.

Tests: `pytest -q`, `ruff check .`, `black --check installer tools tests`, and `shellcheck -x tools/*.sh installer/init/init.sh installer/runtime/lib/*.sh` all pass. `make img`, `make iso`, and `make dist-release` (even with `sudo`) still hit `tools/build_runtime.sh`/`debootstrap` failing to `mknod` `/dev/null` because the workspace is mounted with `nodev`, so the runtime artifacts can’t be materialized here.

Next steps:
1. Re-run the installer media targets on a host where the build directory allows device nodes (or adjust the mount to permit `mknod`) so the runtime build—and thus `make img`/`make iso`/`make dist-release`—can complete end-to-end.