Rebuilt the FAT and metadata module staging loops so matches are collected through explicit glob-safe `for candidate in …` loops, keeping `nullglob` active and preserving the existing warnings while satisfying SC2206.

- Replaced the direct `matches=( … )` assignment in the FAT module loop with an explicit candidate loop, so missing patterns stay empty and ShellCheck no longer sees unquoted expansions (tools/build_installer_initramfs.sh:315).
- Mirrored that same glob-safe collection approach in the metadata copy loop to keep `modules.*` artifacts flowing without SC2206 complaints (tools/build_installer_initramfs.sh:343).

- Tests: `shellcheck tools/build_installer_initramfs.sh`