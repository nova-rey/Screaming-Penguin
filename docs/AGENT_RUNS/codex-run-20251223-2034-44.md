- Shared helper module (`tests/installer/initrd_test_helpers.py:1`) now centralizes `_prepare_kernel_environment`, `_run_installer_initramfs_build`, `_list_initrd_entries`, and `_read_initrd_file`, keeping the stub kernel/modules setup and subprocess failure reporting identical to the previous per-test logic.
- `tests/installer/test_installer_initrd.py:31` and `tests/installer/test_initramfs_by_label_population.py:16` import the shared helpers, drop the duplicated listing/reading code, and read the `init` script directly from the generated initrd so both tests rely on the same deterministic helpers.
- Tests: not run (not requested).

Next steps:
1. Run `pytest -q tests/installer/test_initramfs_by_label_population.py::test_installer_initrd_populates_disk_by_label tests/installer/test_installer_initrd.py` to confirm the refactor keeps the installer initrd tests green.