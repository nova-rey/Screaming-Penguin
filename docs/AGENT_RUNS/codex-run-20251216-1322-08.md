**Test Safety**
- Introduced `DEFAULT_CMD_TIMEOUT_SECONDS` + `SP_TEST_CMD_TIMEOUT_SECONDS` and rewrote `_run_command` so every invocation now prepends optional stub paths, still sources the runtime helpers, waits at most the configured timeout while capturing output, and fails with diagnostics inside `tests/installer/test_bootstrap_dev_nodes.py:11‑55`.
- In `tests/installer/test_bootstrap_dev_nodes.py:85‑154`, the `mdev` invocation is now routed through a temporary `bin`/`sbin` stub (also exported via `SP_BOOTSTRAP_MDEV_BIN` so `sp_bootstrap_dev_nodes` still obeys it after `sp_bootstrap` rewrites `PATH`), both logs the arguments to the existing `SP_TEST_MDEV_LOG` and a new `calls_log`, and the test asserts that the stub actually saw `mdev -s` while the prior `state=dev-bootstrap`/`state=discover-config` ordering checks remain unchanged.

- Tests: `SP_SKIP_INIT_MAIN=1 SP_TEST_CMD_TIMEOUT_SECONDS=120 python3 -m pytest -q tests/installer/test_bootstrap_dev_nodes.py -k mdev` (fails: even with the stub injected, the installer keeps spinning while repeatedly printing `/dev/console` and `/dev/ttyS0` permission errors, so the helper hits the 120 s guard and pytest reports `TimeoutExpired` before the scenario completes in this environment).

Next steps:
1. Re-run the same pytest command inside an environment that provides `/dev/console`/`/dev/ttyS0` access (or the upstream CI image) so the stub-driven flow can finish before the guarded timeout.
2. Once the run succeeds, check the new `calls_log` to ensure `mdev -s` still precedes discovery and adjust `SP_TEST_CMD_TIMEOUT_SECONDS` if you need a tighter bound.