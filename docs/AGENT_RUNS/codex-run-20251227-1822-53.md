- **Fatal path return** Verified `sp_populate_by_label_namespace` already logs the fatal marker, sets `SP_RESCUE_REASON=missing-blkid`, invokes `sp_enter_rescue_mode`, and `return 1` when `blkid` is missing or fails (`installer/init/init.sh:578`, `installer/init/init.sh:585`), so no code changes were required in this helper.
- **Initramfs mirror** The initramfs copy mirrors the same behavior and returns non-zero in the same spots (`build/installer-initramfs/init:578`, `build/installer-initramfs/init:585`), keeping both trees equivalent.
- **Lint** Ran `shellcheck -x installer/init/init.sh build/installer-initramfs/init`; it exited cleanly with no warnings.
- Next steps: 1) if the CI failure persists, re-run the targeted test (`pytest tests/installer/test_by_label_population.py`) to double-check the helper now fails when `SP_BLKID_BIN` is missing.