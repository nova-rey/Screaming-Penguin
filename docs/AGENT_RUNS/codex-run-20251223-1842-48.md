- Centralized the initramfs setup plumbing into `tests/installer/_initramfs_helpers.py#L1` so `_prepare_kernel_environment`, `_run_installer_initramfs_build`, and the shared kernel/module constants can be reused without duplicating setup logic.
- Updated `tests/installer/test_installer_initrd.py#L1` to import those helpers, renamed the build helper usage, and left the module focused on initrd assertions and entry inspection.
- Pointed `tests/installer/test_initramfs_by_label_population.py#L1` at `_initramfs_helpers` for the shared helpers while still pulling `HAS_REQUIRED_TOOLS` from the main initrd test module.
- Tests not run (not requested).

1. Run `pytest -q tests/installer/test_installer_initrd.py` followed by `pytest -q tests/installer/test_initramfs_by_label_population.py` to verify the refactor.