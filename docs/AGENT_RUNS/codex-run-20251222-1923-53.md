Black now passes by reformatting the four installer tests that previously failed `black --check` (see `tests/installer/test_bootstrap_dev_nodes.py:39`, `tests/installer/test_config_discovery_busybox.py:114`, `tests/installer/test_init_write_gate.py:32`, and `tests/installer/test_kernel_module_identity.py:53`), with no changes to runtime logic. A dedicated lint workflow (`.github/workflows/lint.yml:1`) now installs Python 3.11, the required tooling, and runs `ruff check .`, `black --check .`, and `pytest -q` on both pushes and PRs.

- `ruff check .` (pass)
- `black --check .` (pass)
- `pytest -q` (timed out after the 10 minute command limit; the installer suite exceeds that when run locally, so only partial progress was observed before exit 124)

No runtime behavior changes were introduced.

Next steps:
1. Let the new GitHub workflow exercise the full pytest suite (the local run always hit the CLI timeout).
2. For quicker local feedback, consider running a bounded subset (e.g., mirroring the CI smoke filter) until the entire suite can complete within the available time.