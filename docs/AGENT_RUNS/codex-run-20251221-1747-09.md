**Label Fatal**
- Ensured explicit label failures hard-fail immediately after the label probe by logging `[SP-INSTALLER][FATAL]…` and returning 1 when `SP_CONFIG_LABEL_REQUESTED` is true but `SP_CONFIG_LABEL_DEVICE` never populated, so we never slip into rescue mode (`installer/runtime/lib/config_discovery.sh:495`).
- Added `SP_CONFIG_LABEL_REQUESTED` tracking derived from `SP_CONFIG_LABEL`/`SP_CONFIG_LABEL_NAME` so the script knows when a label was explicitly requested before deciding to hard-fail (`installer/runtime/lib/config_discovery.sh:25`).
- Mirrored the same detection and fatal logic inside the initramfs build copy to keep both runtime trees consistent (`build/installer-initramfs/runtime/lib/config_discovery.sh:25`).
- Recorded the investigative notes in the analysis doc and tightened the busybox test to set `SP_CONFIG_LABEL`, expect a non-zero exit, and verify the rescue shell never runs (`docs/analysis/mp-fix-label-fatal.md:1`, `tests/installer/test_config_discovery_busybox.py:166`).

Tests:
- `pytest tests/installer/test_config_discovery_busybox.py`