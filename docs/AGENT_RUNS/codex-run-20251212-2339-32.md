Initramfs now carries a BusyBox payload plus fully POSIX reimage tooling, and the ISO builder actually stages that payload, pulls a host kernel, drops in isolinux, and emits `ouroboros/dist/sp-ouroboros.iso` so the boot medium launches the RAM-only flow.

**Implementation**
- `ouroboros/initramfs_root/init:4` now sets `PATH=/bin:/sbin` and the payload includes `ouroboros/initramfs_root/bin/busybox:1` so every PID‑1 command resolves to BusyBox plus the required utilities that the scripts call.
- The orchestration scripts are rewritten for portable `/bin/sh` so the initramfs can run them directly: `ouroboros/scripts/reimage_usb_from_ram.sh:1`, `ouroboros/scripts/sanity_checks.sh:1`, and `ouroboros/scripts/detect_boot_device.sh:1` avoid Bash-only features while preserving the dry-run gate.
- `ouroboros/tools/make_ouroboros_iso.sh:4` now stages the initramfs (including `ouroboros/scripts`), creates BusyBox symlinks, copies the host kernel with a sudo fallback, writes an isolinux config, and runs `xorriso` to produce `ouroboros/dist/sp-ouroboros.iso:1` labeled `SP_OUROBOROS`.
- Documented the recommended smoke test in `ouroboros/docs/qemu-test.md:1` and logged the analysis/verification plan plus the latest checkpoint notes in `docs/analysis/p1-c1-analysis.md:1`.
- Installed host tooling (`linux-image-5.15.0-1004-kvm`, `xorriso`, `syslinux`, `isolinux`, `qemu-system-x86`, etc.) to supply the kernel, bootloader, and QEMU needed for the build and test run.

**Testing**
- `./ouroboros/tools/make_ouroboros_iso.sh` (generates the ISO with BusyBox initramfs and isolinux layout as above).
- `timeout 120s qemu-system-x86_64 -m 2048 -smp 2 -nographic -serial mon:stdio -no-reboot -cdrom ouroboros/dist/sp-ouroboros.iso -boot d` (kernel + initramfs load, `/init` mounts proc/sys/dev, runs the rewritten sanity and reimage scripts, then `detect_boot_device` exits with “no block device labeled OUROBOROS_BOOT” and drops to the shell—exactly the dry-run failure we expect in this VM).

Next steps:
1. Boot the ISO on hardware or a VM that exposes a block device labeled `OUROBOROS_BOOT` so the detection logic can complete and the reimage dry run reaches its confirmation branch.