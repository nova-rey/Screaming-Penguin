- **Runtime changes:** Added `sp_try_load_fat_modules` so PID‑1 can best-effort modprobe the required NLS/FAT drivers before config discovery; `sp_mount_candidate` now invokes it, captures failed `mount -t vfat` stderr/rc, emits a fatal marker, and rescue mode dumps `/proc/filesystems`/`lsmod` when a config mount fails, improving diagnostics for missing filesystem support (`installer/init/init.sh:463`, `installer/runtime/lib/config_discovery.sh:297`, `installer/runtime/lib/rescue_mode.sh:169`).
- **Test updates:** Introduced shared helpers that recreate the fake kernel image/modules, rebuild the initrd, and gather its entries, then added `test_installer_initrd_includes_vfat_support` to assert the VFAT/NLS blobs and `sp_try_load_fat_modules` are present before config mounting (`tests/installer/test_installer_initrd.py:23`, `tests/installer/test_installer_initrd.py:124`).
- **Supporting artifacts:** Populated the test-kernel staging tree with stub VFAT/NLS modules so the builder can actually copy them into the initrd (`build/runtime-chroot/lib/modules/test-kernel/kernel/fs/fat/fat.ko:1`, `build/runtime-chroot/lib/modules/test-kernel/kernel/fs/fat/vfat.ko:1`, `build/runtime-chroot/lib/modules/test-kernel/kernel/fs/nls/nls_cp437.ko:1`, `build/runtime-chroot/lib/modules/test-kernel/kernel/fs/nls/nls_iso8859-1.ko:1`), and documented the analysis/plan in `docs/analysis/mp-fix-vfat.md:1`.
- **Testing:** `pytest -q tests/installer/test_installer_initrd.py::test_installer_initrd_contains_runtime_payload`, `pytest -q tests/installer/test_installer_initrd.py::test_installer_initrd_includes_vfat_support`.
- **Next steps:** Run `tools/build_runtime.sh`/`tools/build_installer_initramfs.sh` with the full runtime chroot to confirm the real initrd carries the VFAT stack and emits the new diagnostics when the config partition can’t mount.