- `prompt.md:1-45` records the CI run of `shellcheck` across the repo’s shell scripts; the job stops with SC1091/SC2120/SC2181/SC2034/SC2119 warnings (including “Not following: …/disk_layout.sh” and patterns around `sp_select_target_disk`, exit-code checks, unused globals, and argument handling). 
- Fixing the failure means giving `shellcheck` access to `disk_layout.sh` (e.g., run it with `-x` or make the sourced file an explicit input) and addressing the warnings in `installer/runtime/lib/disk_layout.sh` (give `sp_select_target_disk` a meaningful signature/usage, replace `if [ $? -ne 0 ]` with `if ! …`, clarify/export `SP_DISK_LAYOUT_LAST_PLAN`, and pass through positional arguments where needed). 
- After those edits re-run the CI stage to confirm `shellcheck` exits cleanly; if more warnings appear, tackle them in priority order until the job succeeds.