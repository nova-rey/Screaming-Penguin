Bootstrapping now stages the FAT/VFAT/NLS modules, runs `depmod`, and modprobes them before config discovery so the FAT32 `SP_CONFIG` partition can be mounted even on modular kernels, and I recorded the investigation in `docs/analysis/mp-fix-fat-initramfs.md`.

**Changes**
- `tools/build_installer_initramfs.sh:299` now checks that `fat`, `vfat`, `nls_cp437`, and `nls_iso8859-1` appear in the staged module tree, warns if they are absent, and runs `depmod -b …` so `modules.dep`/`modules.alias` exist inside the initramfs.
- `installer/init/init.sh:430` adds `sp_try_modprobe_filesystem_module`/`sp_load_filesystem_modules`, and `sp_run_installer` now calls the new helper (`installer/init/init.sh:988`), with the mirrored copy in `build/installer-initramfs/init:430` staying in sync so the initramfs entrypoint always tries `fat`/`vfat`/`nls` before discovery.
- `docs/CONFIG_DISCOVERY.md:3-7` notes that `SP_CONFIG` is FAT32 and the initramfs loads the filesystem drivers early, and the analysis doc at `docs/analysis/mp-fix-fat-initramfs.md:1-16` captures the findings/plan.
- `tests/installer/test_installer_initrd.py:74-81` now asserts the built init script contains the new `sp_load_filesystem_modules` loop so future regressions are caught.

**Testing**
- `pytest tests/installer/test_config_discovery_busybox.py` (pass; 13 tests, 0 failures).
- `make ci-smoke` (exit 124 / timeout after exercising the same pytest suite; the harness limits the run to ~600 s before killing the process, so the full smoke suite never finished in this environment).

Next steps: rerun `make ci-smoke` (or the full smoke suite) in an environment without the 600 s cap to get a clean completion; otherwise the new logging/warnings should make any missing FAT support visible.