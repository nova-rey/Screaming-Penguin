- Added `sp_populate_by_label_namespace` (installer/init/init.sh:525) to build `/dev/disk/by-label` from sanitized `blkid -o export` records, emit a warning when no labels appear, and treat missing/failing `blkid` as a fatal rescue condition with `SP_RESCUE_REASON=missing-blkid`.
- Hooked that helper into `sp_bootstrap` immediately after `sp_bootstrap_dev_nodes` and mirrored both the definition and call in `build/installer-initramfs/init:525/708`, keeping the runtime and initramfs helpers in sync.
- Updated `sp_run_installer` to invoke the new helper before verifying the namespace (installer/init/init.sh:1237) so config discovery always sees the populated by-label entries.
- Adjusted the initrd build test to look for the new helper, the linking helper, the `ln -sf` usage, and the warn message in `tests/installer/test_initramfs_by_label_population.py:17`.
- Added sandboxed coverage that fakes `blkid` to check symlink creation (`tests/installer/test_by_label_population.py:37`) and that no `blkid` triggers `SP_RESCUE_REASON=missing-blkid` with the fatal log marker (`tests/installer/test_by_label_population.py:85`).

**Tests**
- `shellcheck -x installer/init/init.sh build/installer-initramfs/init`
- `pytest -q tests/installer/test_by_label_population.py`

Consider running the broader installer test suite (e.g., `pytest tests/installer`) if you need additional confidence beyond this focused coverage.