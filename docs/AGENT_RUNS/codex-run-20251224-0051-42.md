- The by-label initrd test now looks for `blkid -o export` either as a literal call or via `$blkid_bin`, accepting the optional `if` prefix so the assertion stays stable whether the init script hard-codes `blkid` or uses the injected binary name (`tests/installer/test_initramfs_by_label_population.py:26`).
- Tests: `pytest -q tests/installer/test_initramfs_by_label_population.py::test_installer_initrd_populates_disk_by_label`.
- ShellCheck: `shellcheck -x installer/init/init.sh installer/runtime/lib/config_discovery.sh installer/runtime/lib/rescue_mode.sh` and `shellcheck -x tools/build_installer_initramfs.sh`.
- Next step option: run the broader CI suite to ensure other components remain green.