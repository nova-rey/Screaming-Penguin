- `installer/init/init.sh:729-867` now chains planner → executor → safety checks → rootfs deploy → bootloader and emits `[SP-INSTALLER] marker=complete`. This block only runs when `SP_ENABLE_DISK_EXECUTE=1` and the new `SP_ENABLE_BOOTLOADER` toggle is set, and the new config/analysis stubs record that gating (also added a permissive `/dev/console` write so tests won’t die on PC systems).
- `installer/runtime/lib/bootloader.sh` is a new helper that mounts EFI, rewrites `/etc/fstab` from `blkid` UUIDs, builds a minimal `/boot/grub/grub.cfg`, chroots to run `grub-install --target=<installer.bootloader.grub_efi_target>`, and watches `SP_SKIP_BOOTLOADER`/`SP_DEBUG_BOOTLOADER` while logging `[SP-INSTALLER] bootloader START/END`; helpers now auto-load defaults via `sp_bootloader_init_config`.
- The runtime helpers (`config_validation.sh`, `safety_checks.sh`, `sp-rootfs-apply.sh`) now resolve `logging.sh` via `SP_RUNTIME_LIB_DIR` so they work when the init shim sources them, and the example config plus schema/contract/architecture/roadmap docs gained the new `installer.bootloader` block, skip/debug flags, and the new `docs/Phase12_Roadmap.md`/`docs/analysis/p12-bootloader-analysis.md` coverage for this checkpoint.
- `tests/installer/test_bootloader.py` drives the bootloader helpers (fstab generation, chroot command assembly, gating behavior) with staged stubs so the CI harness now exercises the new environment without touching real disks.

Tests: `pytest -q`; `ruff check .`; `black --check installer tests`; `shellcheck -x installer/init/init.sh installer/runtime/lib/*.sh`