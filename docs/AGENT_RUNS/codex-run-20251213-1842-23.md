**Ouroboros Sync**
- README now matches `origin/main` and keeps a short pointer to the Ouroboros workspace in the new `## Ouroboros variant` section (`README.md:23`), so the repo-level narrative stays general while still surfacing where the ISO-specific work lives.
- `ouroboros/docs/ouroboros-overview.md:1` now collects the concept, core requirements, script responsibilities, initramfs flow, safety posture, and future hints, ensuring the documentation for this branch lives entirely under `ouroboros/`.
- Root-level duplicates (`scripts/`, `initramfs_root/`, `tools/make_ouroboros_iso.sh`, and `docs/ouroboros-overview.md`) are gone, and the agent log now calls out the canonical paths so future work won’t drift back toward the old scaffold (`docs/AGENT_RUNS/codex-run-20251211-1937-07.md:1`).
- Tests: `pytest -q` (22 passed, 4 skipped).

Next steps: 1) Hook `ouroboros/tools/make_ouroboros_iso.sh` into the real kernel/initramfs builder and asset staging flow so it emits a true ISO, and 2) once an ISO exists, re-run the QEMU verification documented in `ouroboros/docs/qemu-test.md` to exercise the USB-only helpers in a live scenario.