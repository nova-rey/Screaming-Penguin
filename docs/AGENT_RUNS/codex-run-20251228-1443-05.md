- Updated `tests/installer/test_by_label_population.py:100` so the helper invocation records its return code, prints `SP_RESCUE_REASON`, and exits with that code; the test now observes the helper’s failure instead of being masked by the trailing `printf`.

- Captured the helper exit status immediately, which ensures CI sees the non-zero outcome and still reports the rescue reason.

- Ran `shellcheck -x installer/init/init.sh build/installer-initramfs/init` to satisfy the required lint check.