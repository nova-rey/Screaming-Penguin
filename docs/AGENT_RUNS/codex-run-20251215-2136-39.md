- Reworked module staging to treat the runtime-chroot output as the canonical modules root (`RUNTIME_CHROOT_MODULES` now respects `SP_INSTALLER_RUNTIME_CHROOT`/`SP_INSTALLER_RUNTIME_CHROOT_MODULES`) and to log the root/source/destination before checking `MODULES_SRC_DIR`, ensuring the staging line appears before any missing-directory error even in detection-only mode (tools/build_installer_initramfs.sh:9,110-127).  
- Added helper-driven detection coverage for override roots, the runtime-chroot default/override cases, the `SP_INSTALLER_RUNTIME_CHROOT` path, and the detection-mode failure when `MODULES_ROOT/<ver>` is absent, so the new runtime staging logic stays exercised (tests/installer/test_kernel_module_identity.py:37,151).  
- **Tests**  
- `shellcheck tools/build_installer_initramfs.sh`  
- `python3 -m compileall -q .`  
- `python3 -m pytest tests/installer/test_kernel_module_identity.py`  
- Next steps: 1. Run the broader installer build/test pipeline so the new runtime-chroot module staging path is exercised end-to-end.