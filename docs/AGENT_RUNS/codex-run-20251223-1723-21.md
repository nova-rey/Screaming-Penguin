No code change was needed—the FAT/metadata staging loops in `tools/build_installer_initramfs.sh` already wrap their globs in manual `for candidate …` iterations guarded by `nullglob`, so there isn’t any `matches=( ${MODULES_SRC_DIR}/${pattern} )` left to replace. `shellcheck tools/build_installer_initramfs.sh` returns clean, so SC2206 is resolved already. If you still want extra verification, rerun `make ci-smoke` locally.