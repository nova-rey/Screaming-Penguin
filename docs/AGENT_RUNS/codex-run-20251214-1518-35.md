- Replaced the old discovery helper with a BusyBox-only engine that walks `/sys/block`, respects the label→removable→partition order, exports `CONFIG_MOUNT=/config`, and summarises every attempt before handing control to `sp_enter_rescue_mode` when nothing mounts (`installer/runtime/lib/config_discovery.sh:1-380`). Rescue mode now binds `/proc`, `/sys`, and `/dev`, logs `/sys/block` plus `/proc/partitions`, and still executes a BusyBox `sh` while the write gate stays blocked (`installer/runtime/lib/rescue_mode.sh:108-170`).
- Early boot routines now rely solely on sysfs data: `sp_probe_disks` drops the old `lsblk` diagnostics (`installer/init/init.sh:443-485`), and the bootloader queries `/dev/disk/by-uuid` via `readlink` instead of `blkid` while the tests spin up a by-uuid tree for `/dev/mock-root` and `/dev/mock-efi` (`installer/runtime/lib/bootloader.sh:8-205`, `tests/installer/test_bootloader.py:106-220`).
- Documentation/tests reflect the BusyBox philosophy: the analysis note captures the A/B/C requirements (`docs/analysis/option-b-busybox-config-discovery.md:1-35`), new docs explain the discovery order/failure guarantees and the util-linux-free architecture (`docs/CONFIG_DISCOVERY.md:1-23`, `docs/INSTALLER_ARCHITECTURE.md:1-12`), and the replacement suite `tests/installer/test_config_discovery_busybox.py` exercises label/removable/partition paths, rescue mode, and guards against any lingering `blkid`/`lsblk` strings while the old `tests/installer/test_config_discovery.py` was dropped (`tests/installer/test_config_discovery_busybox.py:14-167`).
- Tests: `pytest -q`; `ruff check .`; `black --check installer tools tests`; `shellcheck -x installer/init/init.sh installer/runtime/lib/*.sh tools/*.sh`.

Next steps:
1. Validate the rewritten busybox discovery on an actual initramfs build or hardware image to ensure the new mount/label logic still behaves under real devices.
2. Confirm downstream packaging doesn’t reintroduce util-linux binaries into the early-boot payload if the goal is a pure BusyBox initrd.