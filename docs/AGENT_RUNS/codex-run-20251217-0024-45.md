- Added a `sp_log_fatal_marker` helper plus `sp_find_partition_by_fs_label` so the discovery flow can enumerate `SP_PROC_PARTITIONS`/`SP_DEV_ROOT` entries with `blkid -o export`, store the matching device in `SP_CONFIG_LABEL_DEVICE`, and emit the required `[SP-INSTALLER][FATAL]` markers when either the label scan or `blkid` probe fails (`installer/runtime/lib/config_discovery.sh:30`, `installer/runtime/lib/config_discovery.sh:69`, `installer/runtime/lib/config_discovery.sh:297`).
- Reworked `sp_try_label_candidate` to call the helper without relying on `/dev/disk/by-label`, log the resolver/label/device markers, and surface the probe count when the label is missing (`installer/runtime/lib/config_discovery.sh:297`); `SP_PROC_PARTITIONS` is now configurable so initramfs and tests can drive the enumeration set.
- Extended `tests/installer/test_config_discovery_busybox.py` with helpers that track synthetic `/proc/partitions` entries, stub `blkid` output, and two new scenarios that cover the label probe success and fatal paths while still allowing the rest of the suite to run (`tests/installer/test_config_discovery_busybox.py:42`, `tests/installer/test_config_discovery_busybox.py:147`); the runtime “no util-linux” guard now explicitly permits `blkid` inside `config_discovery.sh`.
- Documented the new `blkid` dependency in the config discovery guide (`docs/CONFIG_DISCOVERY.md:11`) and added the actual `blkid` binary into the initramfs tree so the helper has a real tool to call (`build/installer-initramfs/bin/blkid`).

Tests:
- `pytest tests/installer/test_config_discovery_busybox.py`