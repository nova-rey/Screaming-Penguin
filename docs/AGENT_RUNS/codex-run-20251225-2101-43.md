**Changes**
- Added resilient logging/helpers around `sp_is_by_label_available`/`sp_warn_by_label_unavailable` so missing or empty `/dev/disk/by-label` now just emits a `source=by-label` warning unless `SP_CONFIG_REQUIRE_BY_LABEL=1`, and `sp_try_label_candidate` only rescues in that strict mode while still allowing the blkid label map parser to run (now feeding `blkid -o export` through `sp_parse_blkid_export_to_map` via a temp file) before falling back to partitions (`installer/runtime/lib/config_discovery.sh:4-492`; mirrored into the initramfs copy at `build/installer-initramfs/runtime/lib/config_discovery.sh`).
- Reworked the blkid map builder to capture its output before parsing so we can distinguish missing/unusable `blkid` (fatal when a label is required) from simple “not found” cases, and we now log a clear fatal marker plus enter rescue only when `SP_CONFIG_LABEL_REQUESTED=1` or strict mode dictates (`installer/runtime/lib/config_discovery.sh:177-236`).
- Tightened the BusyBox config-discovery tests to look for the new “continuing fallback discovery” warning and `source=by-label` markers, and added `test_requires_by_label_namespace_strict` so `SP_CONFIG_REQUIRE_BY_LABEL=1` still forces rescue with the new fatal message (`tests/installer/test_config_discovery_busybox.py:372-440`).

**Testing**
- `shellcheck -x installer/runtime/lib/config_discovery.sh build/installer-initramfs/runtime/lib/config_discovery.sh`
- `pytest -q tests/installer/test_config_discovery_busybox.py`
- `pytest -q tests/installer/test_bootstrap_dev_nodes.py::test_bootstrap_runs_mdev_before_config_discovery`
- `make ci-smoke`