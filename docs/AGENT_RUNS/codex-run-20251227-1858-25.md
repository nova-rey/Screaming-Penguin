- Both `installer/init/init.sh:571` and `build/installer-initramfs/init:571` already set `SP_RESCUE_REASON=missing-blkid`, log the fatal marker, call `sp_enter_rescue_mode`, and immediately `return 1`, so the helper now behaves as expected without additional edits.
- Validation: `shellcheck -x installer/init/init.sh build/installer-initramfs/init` ran cleanly.