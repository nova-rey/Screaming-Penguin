- Replaced the `tr`-based FS-type normalization with an IFS-driven loop and rewrote `sp_parse_blkid_export_to_map` to consume `blkid -o export` via a heredoc-backed `while` loop so the runtime discovery path now uses only POSIX shell builtins (`installer/runtime/lib/config_discovery.sh:101`, `installer/runtime/lib/config_discovery.sh:187`).
- Mirrored the same BusyBox-safe FS-type handling and label-map parsing inside the initramfs copy so the payload stays aligned with the runtime behavior (`build/installer-initramfs/runtime/lib/config_discovery.sh:101`, `build/installer-initramfs/runtime/lib/config_discovery.sh:187`).
- Tests: `shellcheck -x installer/runtime/lib/config_discovery.sh`, `shellcheck -x build/installer-initramfs/runtime/lib/config_discovery.sh`.

Next steps:
1. Run the burn test or installer workflow to confirm the previous `tr: not found` crash no longer occurs.