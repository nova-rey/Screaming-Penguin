**Changes**
- `build/installer-initramfs/init:71` now prefers the local `$SP_SCRIPT_DIR/runtime/lib` tree before falling back to the legacy `../runtime/lib`, and `build/installer-initramfs/init:78` logs the resolved directory so initramfs sources its bundled helpers first.
- Added `tests/installer/test_initramfs_runtime_lib_path.py:6` to spin up sandboxed runtime trees, source the init script with the sanitizing env, and assert that only the bundled lib prints out “LOCAL_LIB”.
- Documented that initramfs loads the bundled helper tree at `build/installer-initramfs/runtime/lib` rather than the build root’s `runtime/lib`, making the layout decision explicit in `docs/CONFIG_DISCOVERY.md:5`.

**Testing**
- `pytest -q` (timed out after 600 s with `...............s....` and exit 124 before the full suite completed; it was retried multiple times with the same result).
- `pytest tests/installer/test_initramfs_runtime_lib_path.py -q` (pass).

Next steps: 1) Rerun the full `pytest -q` run with enough time for the suite to finish so the overall regression status is confirmed.