Config discovery now respects `SP_CONFIG_DISCOVERY_EXCLUDE_PREFIXES`, and the BusyBox-focused tests cover the tunable behavior.

- `installer/runtime/lib/config_discovery.sh:245-258` now loops over whatever prefixes are configured before skipping a device, so you can override the default `loop ram fd sr dm` filter without touching the code.
- `build/installer-initramfs/runtime/lib/config_discovery.sh:245-258` mirrors that same helper so the staged initramfs stays consistent with the canonical runtime.
- `tests/installer/test_config_discovery_busybox.py:79-179` gains `_configure_rescue_env` plus `test_respects_exclude_prefix`, keeping the rescue assertions DRY while proving that a prefixed device is skipped and the next removable candidate is used.

- Tests: `pytest tests/installer/test_config_discovery_busybox.py`