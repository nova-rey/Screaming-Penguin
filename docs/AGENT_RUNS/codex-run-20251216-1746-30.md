- Inserted a storage-stack bootstrap right after `sp_bootstrap_dev_nodes`, modprobing the extra controller modules, letting udev/mdev settle for 0.2 s, and calling the new block-device logger (with a fallback `sp_log_block_devices_snapshot`) so disk nodes are enumerated before config discovery (`installer/init/init.sh:386`, `installer/init/init.sh:294`).
- Added `log_block_devices_snapshot` to the runtime logging library so the diagnostic line is emitted via `log_info` and mirrored to `SP_LOG_DEVICE`, keeping the build/initramfs copy congruent (`installer/runtime/lib/logging.sh:43`, `build/installer-initramfs/runtime/lib/logging.sh:43`).

Tests:
- `pytest -q tests/installer/test_bootstrap_dev_nodes.py`

Next steps:
1. Run `make ci-smoke` once the full suite is available to ensure the wider installer flow stays green.
2. Burn and boot the updated initramfs on real hardware so the new block-device visibility can be validated prior to config discovery.