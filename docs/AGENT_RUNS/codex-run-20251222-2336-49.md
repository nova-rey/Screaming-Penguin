Added a dedicated fat-stack preload so the installer initramfs actually stages + loads the VFAT/NLS stack before any config mount and then logs why a VFAT mount failed so rescue mode can shout about it.

- **Loader & capture**: `installer/init/init.sh:463` now defines `sp_try_load_fat_stack` to probe `fat`, `vfat`, `nls_cp437`, and `nls_iso8859-1` with structured `fat-stack` logs before the first `vfat` attempt, and `installer/runtime/lib/config_discovery.sh:345`/`:368` records `SP_CONFIG_LAST_VFAT_MOUNT_OUTPUT` whenever VFAT fails so downstream diagnostics can replay the stderr.
- **Rescue diagnostics**: `installer/runtime/lib/rescue_mode.sh:168-201` now dumps `/proc/filesystems`, `lsmod`, best-effort `modprobe nls_cp437`/`vfat` RCs, and the captured VFAT mount output when the probe exhausted all candidates.
- **Initramfs builder**: `tools/build_installer_initramfs.sh:304-358` explicitly stages the `kernel/fs/fat/*.ko*` and `kernel/fs/nls/*.ko*` blobs, copies any existing `modules.{dep,alias,builtin,order}` metadata, and warns if any required FAT module pattern is missing.
- **Tests & docs**: `tests/installer/test_installer_initrd.py:118` now looks for the new `sp_try_load_fat_stack` and `fat-stack` log strings when inspecting the built init script, and `docs/analysis/mp-fix-vfat-nls-stack.md` summarizes the symptom, hypothesis, fix, and verification steps.

Tests:
- `pytest -q tests/installer/test_installer_initrd.py::test_installer_initrd_includes_vfat_support`