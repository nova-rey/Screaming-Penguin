**Changes**
- updated `sp_check_filesystem_support` in `installer/init/init.sh` to attempt creating `/run/fs-check`, fall back to `/tmp/fs-check`, and then consistently use `${fs_check_dir}/mnt` and `${fs_check_dir}/error` for the VFAT mount probe plus teardown, which keeps diagnostics alive even when `/run` is absent (investigated around `installer/init/init.sh:60-85`).
- mirrored the same diagnostics-directory selection, mounting target, and error capture logic inside `build/installer-initramfs/init` so the initramfs copy stays behaviorally equivalent (`build/installer-initramfs/init:60-85`).

**Testing**
- `shellcheck -x installer/init/init.sh build/installer-initramfs/init` (pass)
- `pytest -q tests/installer/test_filesystem_probe.py` (pass)
- `pytest -q tests/installer/test_init_write_gate.py::test_init_allows_true_write_gate` (fails because the installer run still wants to mount VFAT; as a non-superuser it returns `[SP-INSTALLER][FATAL] VFAT mount test failed` and the test’s assertion at `tests/installer/test_init_write_gate.py:94` sees return code `1` and `mount: /tmp/fs-check/mnt: must be superuser to use mount.`)
- `make ci-smoke` (fails after the same test; the logs show `kernel-modules-missing` + `cannot create /sys/class/scsi_host/host*/scan: Read-only file system`, so the smoke suite stops at `tests/installer/test_init_write_gate.py::test_init_allows_true_write_gate`)

Next steps:
1. Re-run `pytest -q tests/installer/test_init_write_gate.py::test_init_allows_true_write_gate` (and `make ci-smoke`) with root privileges (or inside an environment where `mount -t vfat` and `/sys/class/scsi_host/*/scan` writes are allowed) so the VFAT probe can complete and the write-gate assertions can execute.
2. Once the probe succeeds under privileged execution, re-run `make ci-smoke` to confirm the broader suite passes with the new diagnostics directory behavior.