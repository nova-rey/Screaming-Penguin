Initramfs build now stages the host’s kernel modules and enforces a BusyBox `modprobe`, and the installer `/init` now preloads storage drivers before any config discovery runs.

- `tools/build_installer_initramfs.sh:40` ensures the initramfs tree contains `/lib/modules`, `tools/build_installer_initramfs.sh:122` requires the BusyBox build to expose `modprobe`, and `tools/build_installer_initramfs.sh:166` copies the host’s `/lib/modules/<kernel>` (configurable via `SP_INSTALLER_KERNEL_VERSION`/`_MODULES_SRC`) into the initrd so the installer sees the same drivers it boots with.  
- `installer/init/init.sh:258`–`:300` adds a dedicated storage-driver init that modprobes `ahci`, `sd_mod`, `usb-storage`, and `nvme`, runs `mdev -s`, and logs the results, and `installer/init/init.sh:751` now invokes that step immediately after the bootstrap phase (before mode detection/config discovery) so devices exist before config handling starts.

Tests were not run (not requested).

Next steps:  
1. Rebuild the installer initramfs (`tools/build_installer_initramfs.sh`) and verify `dist/initrd-installer.img` now contains `/lib/modules/<kernel>`.  
2. Boot the image (QEMU or hardware) to confirm `/dev/sd*`/`/dev/nvme*` appear and config discovery succeeds before any write-gate evaluation.