- Planner now resolves the disk itself, logs failures, stores the JSON plan in `SP_DISK_LAYOUT_LAST_PLAN`, and prints it in one place so ShellCheck sees the output path; see `installer/runtime/lib/disk_layout.sh:297` and `installer/runtime/lib/disk_layout.sh:343`.
- `sp_print_layout_plan` just forwards its optional argument through to the planner, leaving the shared logic centralized; see `installer/runtime/lib/disk_layout.sh:350`.
- Init sources the literal `installer/runtime/lib/disk_layout.sh` for ShellCheck and calls the planner with an explicit empty argument so the linter knows `$1` is handled; see `installer/init/init.sh:34` and `installer/init/init.sh:545`.
- Lint: `shellcheck -x installer/runtime/lib/disk_layout.sh installer/init/init.sh`
- Tests: `pytest tests/installer/test_disk_layout_planner.py`