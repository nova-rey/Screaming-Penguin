Refactored `installer/init/init.sh:33` to detect when the script is sourced via `BASH_SOURCE`/`ZSH_EVAL_CONTEXT`, added a dedicated `sp_run_installer` helper at `installer/init/init.sh:827` so `sp_bootstrap` only performs the dev-node work, and now guard the unconditional `main` invocation with that new `SP_SCRIPT_IS_SOURCED` flag at `installer/init/init.sh:970`. With the pipeline split like this, tests can call `sp_bootstrap` without progressing into config discovery, config load, or the write gate.

Moved the partition-handling order in `installer/runtime/lib/config_discovery.sh:359` so we attempt to mount a partition before falling back to reading a directory path, and introduced `SP_TEST_RESCUE_LOOP` in `installer/runtime/lib/rescue_mode.sh:171` so rescue-mode tests can exit after the stub shell finishes. The config-discovery test now sets `SP_TEST_RESCUE_LOOP=0` (`tests/installer/test_config_discovery_busybox.py:136`) and all rescue/init tests pre-create their log files (`tests/installer/test_rescue_mode.py:40`, `tests/installer/test_init_write_gate.py:17`) so `sp_best_effort_redirect` can write there, while `_run_init` also defaults `SP_EXPECTED_KERNEL_VERSION` to whatever exists under `/lib/modules` (`tests/installer/test_init_write_gate.py:27`).

To keep the initrd validation self-contained, the test now stubs a fake kernel and modules tree before invoking `tools/build_installer_initramfs.sh` (`tests/installer/test_installer_initrd.py:26`), and the missing-config bootstrap test exits the rescue loop early without changing real behavior by setting `SP_TEST_RESCUE_LOOP=0`.

Tests:
- `shellcheck -x installer/init/init.sh`
- `pytest -q tests/installer/test_bootstrap_dev_nodes.py::test_bootstrap_runs_mdev_before_config_discovery`
- `pytest -q tests/installer/test_config_discovery_busybox.py::test_falls_back_to_partitions`
- `pytest -q tests/installer/test_config_discovery_busybox.py::test_missing_config_triggers_rescue`
- `pytest -q tests/installer/test_init_write_gate.py`
- `pytest -q tests/installer/test_installer_initrd.py::test_installer_initrd_contains_runtime_payload`
- `pytest -q tests/installer/test_rescue_mode.py::test_rescue_shell_restarts`
- `make ci-smoke`