- **VFAT Guard** Added `sp_check_filesystem_support` to `installer/init/init.sh:60` to probe mounting VFAT and emit fatal diagnostics, then gate the rest of `sp_run_installer` at `installer/init/init.sh:1113` so missing VFAT support now exports `SP_RESCUE_REASON=missing-vfat-support`, enters rescue, and exits before any label/config discovery happens.
- **Initramfs Mirror** Duplicated the helper and the gating check in `build/installer-initramfs/init:60` and `build/installer-initramfs/init:1113` so the shipped initramfs entrypoint and runtime stay aligned.
- **Filesystem Probe Test** New `tests/installer/test_filesystem_probe.py:33` drives `sp_run_installer` with a fake `mount` that returns `EINVAL`, pre-creates the log, pins `SP_EXPECTED_KERNEL_VERSION` to a host module version, and asserts the fatal VFAT log and `rescue-reason=missing-vfat-support` appear while `state=discover-config` never runs.

- **Tests:** `shellcheck -x installer/init/init.sh`, `pytest tests/installer/test_filesystem_probe.py`

Next step: rerun any broader installer or initramfs regression suites to ensure the new probe behaves as expected in more complex environments.