- **Runtime selection** `tools/make_installer_iso.sh:4` now derives the runtime kernel/initrd paths from `RUNTIME_DIR`, checks both exist before falling back to the dist artifacts, and emits the same `[SP-ISO]` diagnostics we had before.
- **Initrd copy** `tools/make_installer_iso.sh:57` now uses `SP_BOOT_INITRD`, ensuring we copy whichever artifact the selector chose while still validating `dist/initrd-installer.img` after the initramfs build.
- **Testing** `pytest -q` (pass), `ruff check .` (pass), `black --check installer tools tests` (fails: `tests/installer/test_installer_iso_initramfs.py` would be reformatted — pre-existing), `shellcheck -x tools/*.sh installer/init/init.sh installer/runtime/lib/*.sh` (pass).

Next steps: 1) Format `tests/installer/test_installer_iso_initramfs.py` if you want `black --check installer tools tests` to pass before any downstream merges.