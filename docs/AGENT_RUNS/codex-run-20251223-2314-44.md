**Changes**
- Added `tests/installer/initrd_test_helpers.py:1`, which centralizes the kernel/module constants plus `_prepare_kernel_environment`, `_run_installer_initramfs_build`, `_list_initrd_entries`, and `_read_initrd_file` so both tests reuse the same deterministic initrd plumbing.
- Updated `tests/installer/test_installer_initrd.py:1` to import those helpers and `tests/installer/test_installer_initrd.py:42` to cast `_list_initrd_entries`’s list output into a set so the existing runtime assertions stay unchanged.
- Pointed `tests/installer/test_initramfs_by_label_population.py:1` at the shared helpers and removed the obsolete `tests/installer/_initramfs_helpers.py:1` file.

**Testing**
- `pytest -q tests/installer/test_initramfs_by_label_population.py::test_installer_initrd_populates_disk_by_label tests/installer/test_installer_initrd.py` — fails because `tests/installer/test_initramfs_by_label_population.py:26` still asserts that `"blkid -o export"` is present, but the built script only records the variable-driven line `"$blkid_bin" -o export` in `installer/init/init.sh:490`, so that exact substring never appears.
- `shellcheck installer/init/init.sh installer/runtime/lib/config_discovery.sh tools/build_installer_initramfs.sh` — exits 1 with the usual SC1091 “not following” messages at `installer/init/init.sh:96` and `installer/runtime/lib/config_discovery.sh:22`, plus SC2086 at `installer/runtime/lib/config_discovery.sh:216` and SC2034 at `installer/runtime/lib/config_discovery.sh:367`.

Next steps
1. Adjust `test_installer_initrd_populates_disk_by_label` (or the built script) so the assertion matches the actual `sp_populate_disk_by_label` implementation instead of the literal `"blkid -o export"` string that no longer exists.
2. Rerun `shellcheck` with `-x` or otherwise resolve the SC1091/SC2086/SC2034 findings once the sourced helpers or quoting issues can be addressed.