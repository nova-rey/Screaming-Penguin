- Added an `SP_INIT_PATH_SAFE` helper so the init script only calls `dirname` with a known value and never triggers nounset failures when `SP_INIT_PATH` is absent, and mirrored the same guard inside the staged init copy that the build process bundles (`installer/init/init.sh:71`, `build/installer-initramfs/init:71`).
- Captured the Block A reasoning around `set -euo pipefail` and how the initramfs build aborts without this fallback in `docs/analysis/mp-fix-sp-init-path-setu.md:1`.
- Tests: `pytest -q tests/installer/test_installer_initrd.py::test_installer_initrd_contains_runtime_payload` (fails because `depmod` rejects the dummy `test-kernel` version with “Bad version passed test-kernel”).
- Tests: `pytest -q tests/installer/test_initramfs_runtime_lib_path.py::test_local_runtime_lib_has_priority` (fails because `build/installer-initramfs/init` wasn’t produced after the preceding build failure, so the sourced script is missing).
- Next steps: 1) Provide a valid kernel version/module setup or adjust the fake-kernel path so `depmod` succeeds and rerun the first pytest; 2) Once the build completes, rerun the second pytest to confirm the runtime-lib priority logic still behaves with the regenerated init script.