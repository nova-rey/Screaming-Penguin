Ensured `sp_populate_by_label_namespace` now honors `SP_BLKID_BIN`, verifies that binary is available before running, logs the “[SP-INSTALLER][FATAL] blkid missing…” message, still warns when no labels exist, and mirrored the same logic in the initramfs copy so the rescue path is deterministic (`installer/init/init.sh:571`, `build/installer-initramfs/init:571`). Updated the by-label population test to force `SP_BLKID_BIN=/no-such-bin/blkid`, assert the helper fails, check for `SP_RESCUE_REASON=missing-blkid`, and look for the new fatal log text so CI no longer relies on `PATH` (`tests/installer/test_by_label_population.py:84`).

Tests:
- `shellcheck -x installer/init/init.sh build/installer-initramfs/init`

Next steps:
1. Run `pytest tests/installer/test_by_label_population.py` locally when convenient to double-check the new failure path.