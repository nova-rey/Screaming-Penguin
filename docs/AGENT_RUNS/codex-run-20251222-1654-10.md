Init now honors the explicit `SP_INIT_PATH` hint when sourced and prefers a bundled `runtime/lib`, while the test and analysis doc reflect the new requirements.

- **Init script** `installer/init/init.sh:71` (and the staged copy at `build/installer-initramfs/init:71`) now derives `SP_SCRIPT_DIR` from `SP_INIT_PATH` when present and chooses `SP_SCRIPT_DIR/runtime/lib` over `../runtime/lib`, keeping the original fallback for boot-time runs.
- **Test harness** `tests/installer/test_initramfs_runtime_lib_path.py:24` sets `SP_INIT_PATH` (alongside the legacy variable) so the sourcing exercise exercises the new path logic and ensures the local runtime/lib output wins.
- **Docs** `docs/analysis/mp-fix-initramfs-runtime-lib-priority.md:1` records the findings and the planned edit strategy for this MP.

Tests:
- `pytest -q tests/installer/test_initramfs_runtime_lib_path.py::test_local_runtime_lib_has_priority` (pass)
- `make ci-smoke` not run (not requested)

Next steps:
1. Run `make ci-smoke` if you want to extend verification beyond the focused pytest.