Enabled `SP_DRY_RUN=1` so `sp_execute_gpt_plan` still enforces the write gate, runs the planner, and logs the dry-run/partitioning skip markers before exiting without touching disks, and wired the unit test to run in that mode instead of asserting physical partitions.

- `installer/runtime/lib/disk_execute.sh#L264-L314`: insert the dry-run guard after plan parsing, emit the requested `[SP-INSTALLER] dry-run=1` / step/result logs, and return success so the function now skips partitioning/mkfs when the flag is set.
- `tests/installer/test_disk_execute.py#L97-L133`: keep the planner assertions but call `sp_execute_gpt_plan` with `SP_DRY_RUN=1`, so the test stays focused on plan generation and the executor entrypoint without needing destructive operations.

- Tests: `pytest -q tests/installer/test_disk_execute.py::test_disk_execute_writes_plan` (skipped because loop devices aren’t available in this environment); `make ci-smoke` (pass; runs the smoke suite).

Running `make ci-smoke` also regenerated several build artifacts/pyc files, so the working tree now lists those other tracked changes.