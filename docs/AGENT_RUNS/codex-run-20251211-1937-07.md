**Ouroboros Scaffold**
- Added the Ouroboros README plus design notes in `ouroboros/README.md:1` and `ouroboros/docs/ouroboros-overview.md:1` so the intent, safety constraints, and future work are clear before any coding begins.
- Introduced guarded shell scaffolds for device detection, sanity checks, and the dry-run reimage flow (`ouroboros/scripts/detect_boot_device.sh:1`, `ouroboros/scripts/sanity_checks.sh:1`, `ouroboros/scripts/reimage_usb_from_ram.sh:1`) plus the minimal ramdisk init launcher at `ouroboros/initramfs_root/init:1`.
- Created the ISO builder stub `ouroboros/tools/make_ouroboros_iso.sh:1` and a repo-level `.gitignore:1`, with `build/.gitkeep:1` and `dist/.gitkeep:1` to hold the empty output directories.

Tests: not run (not requested).

Next steps
1. Implement the actual initramfs build flow (populate the ramdisk, bundle kernel/initrd, and use `ouroboros/tools/make_ouroboros_iso.sh` to produce a real ISO).
2. Validate detection/safety scripts on hardware, then carefully uncomment and enable the destructive imaging commands once the flow is rock-solid.
