CI smoke now prints the installed `pytest` version, runs `tests/installer` via `python3 -m pytest -q`, and the workflow ensures `python3-pytest` is available before invoking the suite.

- The smoke script now logs `pytest`’s version and calls `python3 -m pytest -q tests/installer`, so it no longer depends on an external `pytest` binary (`tools/ci_smoke.sh:18`, `tools/ci_smoke.sh:20`).
- The GitHub Actions smoke job installs `python3-pytest` alongside the other tooling so that the Python entrypoint is satisfied on Ubuntu runners (`.github/workflows/ci.yml:33`–`.github/workflows/ci.yml:39`).
- Tests: Not run (not requested).

Next steps:
1. Run `shellcheck tools/ci_smoke.sh`.
2. Execute `bash tools/ci_smoke.sh` (or `make ci-smoke`) to verify the smoke flow locally.