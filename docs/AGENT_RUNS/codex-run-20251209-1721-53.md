Phase 12 now installs the bootloader within the runtime, gates it behind the new flags, and documents the finish line.

- Implemented `installer/runtime/lib/bootloader.sh`, giving us EFI mount/fstab writers, GRUB cfg creation, and `sp_install_bootloader_and_finalize` that respects `SP_ENABLE_BOOTLOADER`, `SP_SKIP_BOOTLOADER`, and `SP_DEBUG_BOOTLOADER` while emitting `[SP-INSTALLER] bootloader START/END` markers (`installer/runtime/lib/bootloader.sh:171`, `:214`, `:284`, `:319`; documented in `docs/installer_contract.md:17`). 
- `installer/init/init.sh` now only calls the bootloader stage after rootfs succeeds (`installer/init/init.sh:786`) and logs `state=complete` plus a literal `[SP-INSTALLER] COMPLETE` marker once all stages are done (`installer/init/init.sh:809`). 
- The optional `installer.bootloader` block is described in the example config and config validator so the defaults flow into runtime behavior (`config/installer-config.example.yml:20`, `installer/runtime/lib/config_validation.sh:37`), and Phase 12 is captured in the schema/architecture/dev-roadmap/Phase12 roadmap/analysis docs (`docs/CONFIG_SCHEMA.md:90`, `docs/architecture.md:31`, `docs/DEV_ROADMAP.md:562`, `docs/Phase12_Roadmap.md:1`, `docs/analysis/p12-bootloader-analysis.md:1`). 
- Added `tests/installer/test_bootloader.py` to cover fstab generation, chroot command assembly, gating, skip/debug toggles, and the init shim’s path into the bootloader stage (`tests/installer/test_bootloader.py:106`, `:194`). 

- Tests: `pytest -q`; `ruff check .`; `black --check installer tests`; `shellcheck -x installer/init/init.sh installer/runtime/lib/*.sh` (all pass).