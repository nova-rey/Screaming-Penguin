**Result: CHANGED**
- Updated both installer scripts so the blkid failure path now logs “[SP-INSTALLER][FATAL] blkid failed; cannot populate by-label namespace” while still setting `SP_RESCUE_REASON=missing-blkid`, invoking `sp_enter_rescue_mode`, and returning `1`, aligning it with the missing-blkid branch.
- Tests: `shellcheck -x installer/init/init.sh build/installer-initramfs/init`
- Evidence: Preflight `git status --porcelain` output: (none); Preflight `git diff` output: (none); Postflight `git status --porcelain` output:  
  M build/installer-initramfs/init  
  M installer/init/init.sh; Postflight `git diff` output shown below.

```diff
diff --git a/build/installer-initramfs/init b/build/installer-initramfs/init
index b01fb6b..a2f9bab 100755
--- a/build/installer-initramfs/init
+++ b/build/installer-initramfs/init
@@ -583,7 +583,7 @@ sp_populate_by_label_namespace() {
     fi
 
     if ! blkid_output="$("${SP_BLKID_BIN}" -o export 2>/dev/null)"; then
-        sp_log "[SP-INSTALLER][FATAL] blkid missing; cannot populate by-label namespace"
+        sp_log "[SP-INSTALLER][FATAL] blkid failed; cannot populate by-label namespace"
         export SP_RESCUE_REASON="missing-blkid"
         sp_enter_rescue_mode "missing-blkid"
         return 1
diff --git a/installer/init/init.sh b/installer/init/init.sh
index b01fb6b..a2f9bab 100755
--- a/installer/init/init.sh
+++ b/installer/init/init.sh
@@ -583,7 +583,7 @@ sp_populate_by_label_namespace() {
     fi
 
     if ! blkid_output="$("${SP_BLKID_BIN}" -o export 2>/dev/null)"; then
-        sp_log "[SP-INSTALLER][FATAL] blkid missing; cannot populate by-label namespace"
+        sp_log "[SP-INSTALLER][FATAL] blkid failed; cannot populate by-label namespace"
         export SP_RESCUE_REASON="missing-blkid"
         sp_enter_rescue_mode "missing-blkid"
         return 1
```