Initrd builder now stages the runtime helpers, enforces a minimum compressed size, and the accompanying test/doc capture the expected payload so iso/img builds can’t ship a stripped-down artifact.

- **Code** `tools/build_installer_initramfs.sh#L5` now knows where the runtime libs live, stages them into `/runtime/lib`, and aborts with a configurable (default 1 MiB) size check plus `/init` validation before shipping `dist/initrd-installer.img`; `tests/installer/test_installer_initrd.py#L1` rebuilds the archive, unpacks it, and asserts `/init`, the runtime helpers, and the size floor are present to prove the change works.
- **Tests** `pytest -q` (23 passed, 4 skipped), `ruff check .`, `black --check installer tools tests`, `shellcheck -x tools/*.sh installer/init/init.sh installer/runtime/lib/*.sh` – all green.
- **Docs** captured the artifact chain, missing payload symptoms, and files that had to change in `docs/analysis/initrd-bloatfix-analysis.md#L1`.
- **Next steps** 1. Run `make iso` so the ISO builder copies the refreshed `dist/initrd-installer.img` and the GRUB menu boots with `rdinit=/init`. 2. Once loop/sudo access is available, run `make img` or the QEMU media smoke test to verify the FAT32 ESP/BIOS entries ship the same initrd.