- Storage bootstrap now runs the new diagnostics and modprobe sequence right after `sp_bootstrap_dev_nodes`, logging kernel/module metadata, `/sys/block` contents before/after, modprobing the canonical driver list with `log_info`, and emitting the fatal `[SP-INSTALLER][FATAL] kernel-modules-missing …` or `[SP-INSTALLER][FATAL] no-block-devices-after-storage-bootstrap` markers before handing control to the existing rescue flow; the same logic (with `SP_INSTALLER_MODULES_ROOT` used to override `/lib/modules`) is mirrored in `build/installer-initramfs/init:414` to keep the initramfs build in sync (`installer/init/init.sh:414`, `installer/init/init.sh:421`, `installer/init/init.sh:435`, `installer/init/init.sh:458`).
- Added `sp_log_cmd_one_line`, `sp_log_kernel_and_modules_snapshot`, and `sp_log_sys_block_snapshot` helpers so `/lib/modules` and `/sys/block` snapshots can be captured reliably with single-line info/warn entries (`installer/runtime/lib/logging.sh:62`); the `build/installer-initramfs/runtime/lib/logging.sh` copy was updated identically.
- Documented the new diagnostic log lines and fatal markers in `docs/CI.md:65`, and updated `tests/installer/test_bootstrap_dev_nodes.py:97` so the test now provides an `SP_INSTALLER_MODULES_ROOT` subdirectory that matches the running `uname -r`, avoiding the bootstrapping rescue loop while still exercising the new bootstrap path.

- Tests: `shellcheck -x installer/init/init.sh installer/runtime/lib/logging.sh build/installer-initramfs/runtime/lib/logging.sh` (pass); `python3 -m compileall -q .` (pass); `pytest -q tests/installer/test_bootstrap_dev_nodes.py` (pass).