Implemented Phase 13: the raw `.img` now builds a FAT32 ESP with a real installer kernel/initrd and shared GRUB wiring, and the docs/tests/catalogs were updated to record the milestone.

- Reworked `tools/make_installer_img.sh` (lines ~7‑158) to honor configurable `SP_IMG_*` knobs, create GPT with a FAT32 ESP + SP_CONFIG partition, copy the installer kernel/initrd, install `grubx64.efi`/`BOOTX64.EFI`, and drop an `EFI/BOOT/grub.cfg` that points at `/boot/vmlinuz-installer`/`/boot/initrd-installer.img`; the new `tools/grub_shared.sh:1-37` locates the EFI binary and prints the shared kernel/initrd lines so both GRUB paths stay in sync.
- Pointed `tools/make_installer_iso.sh:17-279` at `tools/grub_shared.sh`, reused the helper for the ISO GRUB menu entry, and switched the EFI copy to `sp_resolve_grub_efi_binary`, keeping the BIOS/serial tweaks while sharing the kernel/initrd arguments with the `.img` builder.
- Captured the Phase 13 narrative: `docs/analysis/p13-installer-media-bootability-analysis.md:1-48` (Block A plan), `docs/Phase13_Roadmap.md:1-16`, and the new sections in `docs/DEV_ROADMAP.md:575-585`, `docs/CONFIG_SCHEMA.md:90-106`, `docs/installer_contract.md:10-19`, and `docs/architecture.md:29-39`, plus Bible entries in `docs/SP_BIBLE.md:362-366` and `docs/SCREAMING_PENGUIN_BIBLE.md:362-366` marking the milestone.
- Added `tests/installer/test_installer_media_bootability.py:1-95`, which stages dummy dist artifacts, uses `sudo`/`losetup` (skipping when loop devices can’t be allocated), runs `tools/make_installer_img.sh`, and employs `pyfatfs` (`pip install pyfatfs`) to assert FAT32, `/EFI/BOOT/BOOTX64.EFI`, and the expected `grub.cfg` contents inside the ESP.

Testing:
- `pytest -q` (17 passed, 2 skipped; the new installer-media test skips if `losetup` is unavailable, otherwise it runs under `sudo` and verifies the FAT32 ESP via `pyfatfs`).
- `ruff check .`
- `black --check installer tools tests`
- `shellcheck -x tools/*.sh installer/init/init.sh installer/runtime/lib/*.sh`

Next steps:
1. Run `tools/make_installer_img.sh` (after generating `dist/vmlinuz-installer` + `dist/initrd-installer.img`) on a host with `grub-efi-amd64-bin` and a writable loop device to smoke-test the real USB artifact in QEMU/real hardware.
2. Ensure CI installs `pyfatfs`, `grub-efi-amd64-bin`, and retains loop device access so the new test can exercise the ESP without skipping.