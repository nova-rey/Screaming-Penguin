**Summary**
- Added `SP_SYS_BLOCK_ROOT`/`SP_DEV_ROOT` overrides, refreshed block-device snapshots, and logged the new `[SP-INSTALLER] storage-platform=emmc devices=...` marker before config discovery; the bootstrap now also loads `mmc_block`, `sdhci`, and `sdhci_pci` modules and logs mmc paths for both init and initramfs copies (`installer/init/init.sh:34`, `installer/init/init.sh:298`, `installer/init/init.sh:330`, `installer/init/init.sh:472`, `installer/init/init.sh:506`; mirrored in `build/installer-initramfs/init:34`).
- Extended the shared logging helper to capture `/sys/block/mmcblk*` and `/dev/mmcblk*`, emitting `mmc-sys`/`mmc-dev` fields in both console and `SP_LOG_DEVICE` output, with the change mirrored in the initramfs library (`installer/runtime/lib/logging.sh:43`, `build/installer-initramfs/runtime/lib/logging.sh:43`).
- Added a bootstrap test that fakes a `mmcblk0` sysfs/dev node, points the bootstrap at those roots, and asserts that the eMMC marker appears while the fatal “no-block-devices-after-storage-bootstrap” signal never fires (`tests/installer/test_bootstrap_dev_nodes.py:166`).

**Testing**
- `pytest tests/installer/test_bootstrap_dev_nodes.py`

Consider running `make ci-smoke` to ensure the broader suite stays green with the new detection path.