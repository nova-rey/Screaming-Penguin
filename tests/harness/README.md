# Screaming Penguin QEMU Harness (Placeholder)

This directory will contain the QEMU-based test harness scripts implemented
in Phase 6 Prompt B. The harness will:

- Create and manage virtual disk images under `build/`.
- Launch QEMU using the Screaming Penguin installer image from `dist/`.
- Provide a QEMU-visible `/config` tree for installer runs.
- Capture console logs for installation and post-install boots.

No executable scripts are included in this checkpoint.
