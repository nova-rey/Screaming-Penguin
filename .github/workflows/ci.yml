name: Screaming Penguin CI

on:
  push:
    branches:
      - main
      - master
      - develop
      - "**/feature/**"
  pull_request:

jobs:
  ci:
    name: Lint, Build Image, QEMU Smoke
    runs-on: ubuntu-latest

    env:
      DEBIAN_FRONTEND: noninteractive

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show environment info
        run: |
          uname -a
          lsb_release -a || true
          echo "Shell: $SHELL"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            shellcheck \
            qemu-system-x86 \
            parted \
            kpartx \
            dosfstools \
            util-linux \
            mount \
            xz-utils

      - name: ShellCheck all shell scripts
        run: |
          echo "[CI] Running ShellCheck on all *.sh files..."
          SH_FILES="$(git ls-files '*.sh')"
          if [ -n "$SH_FILES" ]; then
            shellcheck $SH_FILES
          else
            echo "[CI] No shell scripts found; skipping ShellCheck."
          fi

      - name: Install ISO build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y debootstrap

      - name: Build installer image (make iso)
        run: |
          echo "[CI] Running 'make iso' to build Screaming Penguin image..."
          sudo make iso
          # Ensure build/ and dist/ are owned by the current user, not root.
          if [ -d build ]; then
            sudo chown -R "$USER:$USER" build
          fi
          if [ -d dist ]; then
            sudo chown -R "$USER:$USER" dist
          fi
          ls -lh dist || true

      - name: QEMU smoke boot (BIOS)
        run: |
          echo "[CI] Running QEMU smoke test..."
          chmod +x ci/qemu_smoke_ci.sh
          ci/qemu_smoke_ci.sh

  # ISO CI job:
  # Builds a hybrid BIOS/UEFI-bootable ISO using grub-mkrescue.
  # Does not perform QEMU runtime tests yet.
  # Produces screaming-penguin.iso as an artifact.
  iso-build:
    name: "ISO Build and Artifact"
    runs-on: ubuntu-latest
    needs: [ci]
    env:
      DEBIAN_FRONTEND: noninteractive
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies for ISO build
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            xorriso \
            isolinux \
            syslinux-common \
            grub-pc-bin \
            grub-efi-amd64-bin \
            grub-efi-amd64-signed \
            mtools \
            debootstrap

      - name: Build ISO image
        run: |
          echo "[CI-ISO] Running 'make iso'..."
          sudo make iso
          echo "[CI-ISO] Listing dist/ contents…"
          ls -lh dist || true

      - name: Upload ISO artifact
        uses: actions/upload-artifact@v4
        with:
          name: screaming-penguin-iso
          path: dist/*.iso
          if-no-files-found: error
